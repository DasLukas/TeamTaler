/** Minimum length accepted when a user chooses a new password. */
export const PASSWORD_MIN_LENGTH = 12;

/** Maximum password length accepted by the authentication contract. */
export const PASSWORD_MAX_LENGTH = 1024;

