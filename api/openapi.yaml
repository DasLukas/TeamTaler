openapi: 3.1.0
info:
  title: TeamTaler API
  version: 0.9.0
  description: |
    Same-origin JSON API for TeamTaler. Monetary values use integer minor units:
    response fields are exact decimal strings, while command inputs are bounded
    JSON integers.
    State-changing authenticated requests require the `X-CSRF-Token` header.
    Product creation, invitation CSV imports, raw-data export creation, and financial mutations that
    create or reverse bookings, payments, or periods additionally require an
    `Idempotency-Key`. A batch-booking idempotency scope includes every managed
    guest created by that command. Versioned role and assignment replacement
    or deletion, public join-link updates, system-setting and SMTP revision
    mutations, provisioning-invitation replacement, and global group lifecycle
    commands require a strong `If-Match` ETag where they target an existing
    versioned resource.
  license:
    name: AGPL-3.0-only
    identifier: AGPL-3.0-only
servers:
  - url: /api/v1
security:
  - sessionCookie: []
tags:
  - name: Authentication
  - name: Authorization
  - name: System
  - name: Groups
  - name: Catalog
  - name: Activities
  - name: Bookings
  - name: Finance
  - name: Periods
  - name: Exports
  - name: Operations
paths:
  /auth/login:
    post:
      tags: [Authentication]
      summary: Create a local session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginCommand'
      responses:
        '200':
          description: Authenticated session
          headers:
            Set-Cookie:
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '401': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /auth/capabilities:
    get:
      tags: [Authentication]
      summary: Read public account-recovery capabilities
      description: |
        Reports which email-dependent account actions are available in the
        current deployment. The values are derived from complete SMTP and
        authenticated token-encryption configuration; clients may use them to
        hide unavailable entry points. They are capability hints only and do
        not replace server-side checks on later commands.
      security: []
      responses:
        '200':
          description: Current public authentication capabilities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthenticationCapabilities'
  /instance/capabilities:
    get:
      tags: [System]
      summary: Read non-sensitive effective instance capabilities
      description: |
        Public presentation and policy hints. The response contains no setting
        source metadata, SMTP data, account information, or authorization
        state. Clients must still handle policy changes on later commands.
      security: []
      responses:
        '200':
          description: Current public instance capabilities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InstanceCapabilities'
  /auth/password-reset/request:
    post:
      tags: [Authentication]
      summary: Request a password-reset email
      description: |
        Returns the same empty `202` response for known and unknown addresses.
        For an eligible account, the service creates a one-hour, single-use
        request and queues its encrypted token for transactional email
        delivery. The email URL carries the plaintext token only in its
        fragment; the client submits it in the confirmation request body.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetRequest'
      responses:
        '202': { description: Request accepted without disclosing account existence }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /auth/password-reset/confirm:
    post:
      tags: [Authentication]
      summary: Consume a password-reset token
      description: |
        Replaces the password for the token's account, consumes the token once,
        cancels every other open account-action request, and revokes all
        sessions for that user. The token must come from the reset page URL
        fragment and must never be placed in a request path or query string.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetConfirmation'
      responses:
        '204': { description: Password replaced and all sessions revoked }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /auth/email-change/confirm:
    post:
      tags: [Authentication]
      summary: Consume an email-change token
      description: |
        Replaces the account email only after the mailbox token is verified,
        consumes that token once, cancels every other open account-action
        request, and revokes all sessions for the user. User, membership,
        balance, statement, and audit identifiers remain unchanged. The token
        must come from the confirmation page URL fragment.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountActionTokenCommand'
      responses:
        '204': { description: Email replaced and all sessions revoked }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /auth/logout:
    post:
      tags: [Authentication]
      summary: Revoke the current session
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Session revoked }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
  /session:
    get:
      tags: [Authentication]
      summary: Read the current session and group memberships
      responses:
        '200':
          description: Current session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '401': { $ref: '#/components/responses/Problem' }
  /permission-definitions:
    get:
      tags: [Authorization]
      summary: List stable permission definitions and implication metadata
      description: |
        Returns the application-defined permission registry. Every definition
        currently reports `GROUP` as its only allowed scope. `CATEGORY` and
        `PRODUCT` remain forward-compatible grant shapes, and v1 role mutations
        reject them.
      responses:
        '200':
          description: Stable permission definitions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/PermissionDefinition' }
        '401': { $ref: '#/components/responses/Problem' }
  /me:
    get:
      tags: [Authentication]
      summary: Read the current session and group memberships
      description: Compatibility alias for `/session`.
      responses:
        '200':
          description: Current session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '401': { $ref: '#/components/responses/Problem' }
  /me/appearance:
    put:
      tags: [Authentication]
      summary: Update the current account's color mode
      description: Persists SYSTEM, LIGHT, or DARK for the authenticated account across devices.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/Appearance' }
      responses:
        '200':
          description: Persisted account appearance
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Appearance' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /me/profile:
    patch:
      tags: [Authentication]
      summary: Update the current user's display name
      description: Updates the global account name without changing memberships, accounting history, or sessions.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProfileUpdate'
      responses:
        '200':
          description: Updated current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /me/group-preference:
    put:
      tags: [Authentication]
      summary: Select the group opened after login
      description: |
        A group identifier selects a fixed default from the current user's
        active memberships. Null selects the most recently used active group.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupPreferenceUpdate' }
      responses:
        '200':
          description: Persisted default-group preference
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupPreference' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /me/group-preference/last-used:
    put:
      tags: [Authentication]
      summary: Record the current group as most recently used
      description: Accepts only a group with an active membership for the current user.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/LastUsedGroupUpdate' }
      responses:
        '204': { description: Most recently used group recorded }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /me/password:
    put:
      tags: [Authentication]
      summary: Replace the current user's password
      description: |
        Requires the current password. On success every session for the user,
        including the current session, is revoked.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordUpdate'
      responses:
        '204': { description: Password replaced and all sessions revoked }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /me/email-change:
    post:
      tags: [Authentication]
      summary: Request a verified email-address change
      description: |
        Requires the current password. The current address remains active until
        a one-hour, single-use token sent to `newEmail` is confirmed. Complete
        SMTP and authenticated token-encryption configuration is required. The
        accepted response does not reveal whether another account already uses
        the requested address; in that case no action or email is created.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmailChangeRequest'
      responses:
        '202':
          description: Verification email queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationRequired'
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /me/push-subscriptions:
    get:
      tags: [Authentication]
      summary: List redacted browser push devices for the current account
      responses:
        '200':
          description: Active account-owned devices without endpoints or browser keys
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [items]
                properties:
                  items: { type: array, maxItems: 10, items: { $ref: '#/components/schemas/PushSubscriptionDevice' } }
        '401': { $ref: '#/components/responses/Problem' }
    post:
      tags: [Authentication]
      summary: Reconcile the current browser push subscription
      description: The endpoint and browser keys are validated, encrypted, and never returned. An endpoint already used in a shared browser profile is atomically reassigned to the current account.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PushSubscriptionRegistration' }
      responses:
        '201':
          description: Redacted current device
          content: { application/json: { schema: { $ref: '#/components/schemas/PushSubscriptionDevice' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /me/push-subscriptions/{subscriptionId}:
    parameters:
      - name: subscriptionId
        in: path
        required: true
        schema: { type: string }
    patch:
      tags: [Authentication]
      summary: Rename an owned browser push device
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [label]
              properties: { label: { type: string, minLength: 1, maxLength: 80 } }
      responses:
        '200':
          description: Renamed redacted device
          content: { application/json: { schema: { $ref: '#/components/schemas/PushSubscriptionDevice' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Authentication]
      summary: Revoke an owned browser push device
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Device revoked }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
  /me/notifications/{notificationId}/destination:
    get:
      tags: [Authentication]
      summary: Resolve an owned opaque notification to its group
      description: |
        Supports privacy-minimized Web Push navigation without placing a group
        identifier in the external payload. Unknown, inaccessible, inactive,
        and archived destinations all return the same `404` response.
      parameters:
        - name: notificationId
          in: path
          required: true
          schema: { type: string, minLength: 1 }
      responses:
        '200':
          description: Current active group containing the owned notification
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [groupId]
                properties:
                  groupId: { type: string }
        '401': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
  /me/avatar:
    post:
      tags: [Authentication]
      summary: Validate, normalize, and set the current user's profile image
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [image]
              properties:
                image:
                  type: string
                  format: binary
      responses:
        '200':
          description: Protected content-addressed profile-image URL
          content:
            application/json:
              schema:
                type: object
                required: [avatarUrl]
                properties:
                  avatarUrl: { type: string }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Authentication]
      summary: Remove the current user's profile image
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Profile image removed }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
  /users/{userId}/avatar/{imageKey}:
    get:
      tags: [Authentication]
      summary: Read the current profile image of a user sharing a group
      parameters:
        - $ref: '#/components/parameters/UserId'
        - name: imageKey
          in: path
          required: true
          schema:
            type: string
            pattern: '^[0-9a-f]{64}\.png$'
      responses:
        '200':
          description: Normalized profile image
          content:
            image/png:
              schema: { type: string, format: binary }
        '401': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
  /invitations/preview:
    post:
      tags: [Authentication]
      summary: Resolve safe onboarding hints for a valid invitation token
      description: Rate-limited public lookup that never exposes the invited email address, roles, or category grants.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/InvitationPreviewCommand' }
      responses:
        '200':
          description: Safe invitation preview
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InvitationPreview' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /invitations/accept:
    post:
      tags: [Authentication]
      summary: Consume an invitation and create or validate an account
      description: |
        Ordinary invitations create or reactivate a membership with their
        explicit role selection. A temporary-guest claim invitation instead
        attaches credentials or an authenticated existing account to its
        `targetMembershipId`, preserves that membership's accounting identity,
        and applies exactly the invitation's stored roles. A conflicting
        existing membership returns `409` rather than
        merging financial histories. The client must echo the previewed account
        state. If another invitation creates the same global account before
        acceptance, this endpoint returns the typed `invitation-account-state-changed`
        conflict without consuming the token. The client then reloads the
        preview and requests the current account password. Open invitations for
        one canonical email address are atomically bound to the first account
        created for that mailbox; their group-specific roles remain independent.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvitationAcceptance'
      responses:
        '201':
          description: Joined group and authenticated session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '401': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /public-join-links/preview:
    post:
      tags: [Authentication]
      summary: Resolve safe group metadata for a valid public join link
      description: |
        Rate-limited public lookup. The opaque join token is supplied from the
        URL fragment by the client and is never included in server access-log
        URLs. The response exposes neither role data nor group identifiers.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PublicJoinTokenCommand' }
      responses:
        '200':
          description: Safe public join-link preview
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PublicJoinPreview' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /public-join-links/registrations:
    post:
      tags: [Authentication]
      summary: Start an email-verified account registration through a public link
      description: |
        Queues a one-time verification email for a new account. The accepted
        response is deliberately identical when the normalized email already
        belongs to an account, preventing account enumeration. A membership is
        created only after verification and receives the group's current
        default role at that later point in time.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PublicJoinRegistration' }
      responses:
        '202':
          description: Verification processing accepted without disclosing account existence
          content:
            application/json:
              schema: { $ref: '#/components/schemas/VerificationRequired' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /public-join-links/registrations/resend:
    post:
      tags: [Authentication]
      summary: Replace and resend a pending public registration proof
      description: Returns the same accepted response when no matching pending registration exists.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PublicJoinResendCommand' }
      responses:
        '202':
          description: Verification processing accepted
          content:
            application/json:
              schema: { $ref: '#/components/schemas/VerificationRequired' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /public-join-links/registrations/confirm:
    post:
      tags: [Authentication]
      summary: Verify a public registration and create its account and membership
      description: |
        Atomically consumes the one-time mailbox proof, creates the account and
        membership, assigns the group's current default role, and creates a
        session. Link rotation or deactivation invalidates every pending proof.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PublicJoinVerificationCommand' }
      responses:
        '201':
          description: Verified account, membership, and authenticated session
          headers:
            Set-Cookie:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Session' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /public-join-links/accept:
    post:
      tags: [Authentication]
      summary: Join through a public link with the current authenticated account
      description: |
        Active memberships are returned idempotently. Archived memberships are
        reactivated after their role set is atomically replaced by the group's
        current default role.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PublicJoinTokenCommand' }
      responses:
        '200':
          description: Joined group and refreshed session view
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Session' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /system/settings:
    get:
      tags: [System]
      summary: Read effective instance settings
      description: Requires a current `SYSTEM_ADMINISTRATOR` database assignment.
      responses:
        '200':
          description: Effective settings and their current aggregate version
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemSettings' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
    patch:
      tags: [System]
      summary: Persist instance-setting overrides
      description: |
        Only supplied fields change. A persisted override wins over its current
        environment and code defaults. Authorization and the ETag are
        revalidated in the serialized write transaction.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SystemSettingsUpdate' }
      responses:
        '200':
          description: Updated effective settings
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemSettings' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /system/settings/reset:
    post:
      tags: [System]
      summary: Remove selected persisted setting overrides
      description: The effective value immediately falls back to the current environment or code default.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SystemSettingsReset' }
      responses:
        '200':
          description: Effective settings after reset
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemSettings' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /system/settings/smtp:
    put:
      tags: [System]
      summary: Apply persisted SMTP configuration overrides
      description: |
        Applies supplied fields to the TLS-only SMTP override document. A content change creates
        an untested revision and leaves it disabled. Enabling is accepted only
        for the otherwise unchanged current revision after its test succeeds.
        The password is write-only; omit it only to retain an already persisted
        password.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SystemSMTPUpdate' }
      responses:
        '200':
          description: Effective settings with the new disabled SMTP revision
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemSettings' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [System]
      summary: Remove the persisted SMTP override
      description: Restores the current complete environment SMTP configuration or disabled code default.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '200':
          description: Effective settings after SMTP reset
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemSettings' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /system/settings/smtp/test:
    post:
      tags: [System]
      summary: Test the current effective SMTP configuration
      description: |
        Sends a test message to the current system administrator account or an
        immutable operator-configured test recipient, then marks only an
        unchanged persisted revision as tested. A host-provided configuration
        is exercised without creating revision state. This command is rate
        limited and never returns SMTP credentials or the recipient override.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '200':
          description: SMTP test succeeded; persisted settings carry a new aggregate revision when verification was required
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemSettings' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /system/settings/web-push:
    put:
      tags: [System]
      summary: Apply persisted Web Push configuration overrides
      description: Updates the redacted VAPID configuration. Replacing an existing private key requires explicit rotation confirmation and causes browsers to register against the new public key identifier.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SystemWebPushUpdate' }
      responses:
        '200':
          description: Effective redacted settings
          headers: { ETag: { schema: { type: string } } }
          content: { application/json: { schema: { $ref: '#/components/schemas/SystemSettings' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [System]
      summary: Remove persisted Web Push overrides
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '200':
          description: Effective settings after reset
          headers: { ETag: { schema: { type: string } } }
          content: { application/json: { schema: { $ref: '#/components/schemas/SystemSettings' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /system/settings/web-push/generate-key:
    post:
      tags: [System]
      summary: Generate and persist a VAPID identity
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                confirmRotation: { type: boolean, default: false }
      responses:
        '200':
          description: Effective redacted settings with the generated public key
          headers: { ETag: { schema: { type: string } } }
          content: { application/json: { schema: { $ref: '#/components/schemas/SystemSettings' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /system/settings/web-push/test:
    post:
      tags: [System]
      summary: Send a privacy-minimized test to an administrator device
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                subscriptionId: { type: string }
      responses:
        '200':
          description: Test request accepted by the browser push service
          content: { application/json: { schema: { $ref: '#/components/schemas/SystemSettings' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /system/administrators:
    get:
      tags: [System]
      summary: List global system administrators
      description: Read-only HTTP projection. Role assignment is available only through the local CLI.
      responses:
        '200':
          description: Current global assignments
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/SystemAdministrator' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
  /system/accounts:
    get:
      tags: [System]
      summary: Search credentialed local accounts for group provisioning
      parameters:
        - name: q
          in: query
          required: false
          schema: { type: string, minLength: 1, maxLength: 254 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
      responses:
        '200':
          description: Matching accounts without group-domain data
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/SystemAccount' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /system/groups:
    get:
      tags: [System]
      summary: List every group and lifecycle impact summary
      responses:
        '200':
          description: Global lifecycle list
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/SystemGroup' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
    post:
      tags: [System]
      summary: Create or provision a group with its initial administrator
      description: |
        An existing account produces an active group immediately. An unknown
        email address produces a provisioning group plus a protected
        first-administrator invitation and returns its one-time acceptance URL.
        When effective SMTP is active, the same invitation is also queued for
        email delivery. The acting system administrator is never joined implicitly.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SystemGroupCreate' }
      responses:
        '201':
          description: Active or provisioning group
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemGroupInvitationResult' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /system/groups/{groupId}/archive:
    post:
      tags: [System]
      summary: Archive an active or provisioning group
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '200':
          description: Archived group
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemGroup' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /system/groups/{groupId}/logo:
    get:
      tags: [System]
      summary: Read the current group logo for the system group list
      description: This resource exposes only the group logo and grants no access to other group-scoped data.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Normalized PNG group logo
          content:
            image/png:
              schema: { type: string, format: binary }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
  /system/groups/{groupId}/restore:
    post:
      tags: [System]
      summary: Restore an archived group without re-enabling old external links
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '200':
          description: Group restored to its active or provisioning pre-archive state
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemGroup' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /system/groups/{groupId}/invitation/resend:
    post:
      tags: [System]
      summary: Replace a provisioning group's first-administrator invitation
      description: |
        Requires a live `SYSTEM_ADMINISTRATOR` assignment and is available only
        while the group is `PROVISIONING`. The
        command atomically revokes every open invitation for the group, cancels
        its pending or retryable invitation-email work, creates a fresh
        seven-day first-administrator invitation with a newly generated token,
        returns its one-time acceptance URL, optionally queues encrypted email
        delivery when SMTP is active, and increments the managed-group version.
        Every older invitation link remains invalid.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '200':
          description: Managed provisioning group after invitation replacement
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemGroupInvitationResult' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /system/groups/{groupId}/deletion-impact:
    get:
      tags: [System]
      summary: Read permanent-deletion impact counts before archival or purge
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Current impact counts
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupDeletionImpact' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
  /system/groups/{groupId}/purge:
    post:
      tags: [System]
      summary: Permanently purge an archived group
      description: |
        Requires a current strong ETag and the exact current group name. Only a
        minimal global deletion receipt remains in system audit.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupPurgeCommand' }
      responses:
        '200':
          description: Deleted-row and media impact retained in the minimal global audit receipt
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupDeletionImpact' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /system/audit:
    get:
      tags: [System]
      summary: List append-only global system audit events
      description: |
        Filters, search, sorting, and keyset pagination execute against the full
        authorized system audit table. The existing object response shape is
        retained. Date-only bounds use UTC: `occurredFrom` starts at the selected
        day's 00:00, while `occurredTo` includes the complete selected day by
        becoming an exclusive bound at the next day's 00:00. RFC 3339 upper
        timestamps are exclusive. `q` searches actor name, action, resource type,
        resource identifier, and serialized safe metadata.
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/TableSearch'
        - $ref: '#/components/parameters/TableCursor'
        - $ref: '#/components/parameters/SortDirection'
        - name: actorUserId
          in: query
          schema: { type: string }
        - name: action
          in: query
          description: Exact action values combined with OR semantics when repeated.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 100
            items: { type: string, maxLength: 200 }
        - name: resourceType
          in: query
          description: Exact resource-type values combined with OR semantics when repeated.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 100
            items: { type: string, maxLength: 200 }
        - name: occurredFrom
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: occurredTo
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: sort
          in: query
          schema: { type: string, enum: [occurredAt, actorName, action, resourceType], default: occurredAt }
      responses:
        '200':
          description: Newest global events, including minimal purge receipts
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; omitted on the final page.
              schema: { type: string }
            X-Has-More:
              description: Whether another matching page exists.
              schema: { type: boolean }
            X-Page-Limit:
              description: Effective maximum number of rows in this page.
              schema: { type: integer, minimum: 1, maximum: 200 }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/SystemAuditEvent' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /system/audit/filter-options:
    get:
      tags: [System]
      summary: List system-audit filter options
      description: Returns every distinct action and resource type currently present in the authorized system audit log. Newly recorded values appear automatically.
      responses:
        '200':
          description: Complete data-derived system-audit filter catalog
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AuditFilterOptions' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
  /system/table-exports:
    post:
      tags: [Exports]
      summary: Export the authorized system-audit table
      description: |
        Requires a current `SYSTEM_ADMINISTRATOR` assignment and accepts only
        the `SYSTEM_AUDIT` table identifier. The server replays the table's
        validated filters and deterministic ordering across every matching row;
        UI pagination, action columns, client-supplied rows, titles, and columns
        are never part of the command. This endpoint is not a system-wide raw
        data export.

        CSV is bounded to 100,000 rows, 100 MiB, and 120 seconds. PDF is bounded
        to 10,000 rows, 50 MiB, and 60 seconds. A bound returns Problem Details
        without a truncated document.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SystemTableExportCommand' }
      responses:
        '200':
          description: Complete localized table download
          headers:
            Content-Disposition:
              schema: { type: string }
            Content-Length:
              schema: { type: integer, format: int64, minimum: 0 }
          content:
            text/csv:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups:
    get:
      tags: [Groups]
      summary: List groups available to the current user
      responses:
        '200':
          description: Active group memberships
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Group' }
    post:
      tags: [System]
      summary: Create a group through the system-administration compatibility alias
      description: |
        Compatibility alias for `POST /system/groups`. It requires a live
        `SYSTEM_ADMINISTRATOR` assignment and never joins the acting account
        implicitly.
      parameters:
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SystemGroupCreate' }
      responses:
        '201':
          description: Active or provisioning group
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SystemGroupInvitationResult' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}:
    patch:
      tags: [Groups]
      summary: Update group identity with GROUP_ADMINISTRATION
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, minLength: 1, maxLength: 120 }
      responses:
        '200':
          description: Persisted normalized group settings
          content:
            application/json:
              schema:
                type: object
                required: [name]
                properties:
                  name: { type: string }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/table-exports:
    post:
      tags: [Exports]
      summary: Export one authorized group or personal table
      description: |
        Accepts Activities, Payments, Account balances, Group settlements,
        Personal settlements, Active members, Archived members, or Group audit.
        The server applies the selected table's existing live authorization and
        replays its validated filters and deterministic ordering across every
        matching row. The export never widens access, follows UI pagination, or
        accepts client-supplied rows, titles, columns, or action controls.

        CSV is bounded to 100,000 rows, 100 MiB, and 120 seconds. PDF is bounded
        to 10,000 rows, 50 MiB, and 60 seconds. A bound returns Problem Details
        without a truncated document. PDFs use A4 landscape and repeat the
        validated local group logo or TeamTaler fallback, canonical title,
        localized export time, page `n/m`, and table headings on every page.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupTableExportCommand' }
      responses:
        '200':
          description: Complete localized table download
          headers:
            Content-Disposition:
              schema: { type: string }
            Content-Length:
              schema: { type: integer, format: int64, minimum: 0 }
          content:
            text/csv:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/exports:
    post:
      tags: [Exports]
      summary: Queue a complete structured group-data archive
      description: |
        Requires a live active membership with `GROUP_ADMINISTRATION` and
        reauthentication with the current account password. The resulting
        group archive intentionally includes every structured group-owned
        dataset, including finance data, regardless of narrower operational
        permissions. It contains UTF-8 CSV datasets with stable English
        technical headers, `manifest.json`, and `schema.json`, but no image,
        receipt, attachment, session, credential, secret, token, or other media
        bytes. TeamTaler exposes no system-wide raw-data archive.

        The idempotent asynchronous job is owned by the requesting actor. At
        most one group-scope job may be queued or running per actor and group,
        and no more than five group-scope starts per actor are accepted per
        hour. A ready artifact expires after 24 hours.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DataExportCreateCommand' }
      responses:
        '202':
          description: New or idempotently replayed actor-owned export job
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DataExportJob' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/me/exports:
    post:
      tags: [Exports]
      summary: Queue the current actor's structured group-data archive
      description: |
        Requires an active membership in the selected current group and
        reauthentication with the current account password. The archive
        contains the actor's profile-related data plus only the actor's own
        structured records in that group. It does not include records from any
        other group or image, receipt, attachment, session, credential, secret,
        token, or other media bytes.

        The idempotent asynchronous job is owned by the requesting actor. At
        most one personal-scope job may be queued or running per actor and
        group, and no more than five personal-scope starts per actor are
        accepted per hour. A ready artifact expires after 24 hours.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DataExportCreateCommand' }
      responses:
        '202':
          description: New or idempotently replayed actor-owned export job
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DataExportJob' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '429': { $ref: '#/components/responses/Problem' }
  /exports:
    get:
      tags: [Exports]
      summary: List the current actor's export jobs in one group
      description: |
        Returns up to 100 newest jobs requested by the authenticated account in
        the selected group. Group administrators cannot inspect another actor's
        jobs through this endpoint.
      parameters:
        - name: groupId
          in: query
          required: true
          schema: { type: string, minLength: 1 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
      responses:
        '200':
          description: Actor-owned job history
          content:
            application/json:
              schema:
                type: array
                maxItems: 100
                items: { $ref: '#/components/schemas/DataExportJob' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /exports/{exportId}:
    get:
      tags: [Exports]
      summary: Read one actor-owned export job
      parameters:
        - $ref: '#/components/parameters/ExportId'
      responses:
        '200':
          description: Current durable lifecycle state
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DataExportJob' }
        '401': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Exports]
      summary: Cancel or delete one actor-owned export artifact
      description: |
        Cancels a queued, running, or ready actor-owned export. Any published
        artifact is removed. A failed export cannot be cancelled.
      parameters:
        - $ref: '#/components/parameters/ExportId'
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Job cancelled and any artifact removed }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /exports/{exportId}/download:
    get:
      tags: [Exports]
      summary: Download one ready actor-owned raw-data archive
      description: |
        Rechecks the current active membership and scope permission before
        opening the complete artifact. Only `READY`, unexpired jobs owned by the
        authenticated actor are downloadable.
      parameters:
        - $ref: '#/components/parameters/ExportId'
      responses:
        '200':
          description: Complete structured CSV ZIP archive
          headers:
            Content-Disposition:
              schema: { type: string }
            Content-Length:
              schema: { type: integer, format: int64, minimum: 0 }
            Last-Modified:
              schema: { type: string }
            X-Content-SHA256:
              description: Lowercase hexadecimal SHA-256 digest of the complete ZIP artifact.
              schema: { type: string, pattern: '^[a-f0-9]{64}$' }
          content:
            application/zip:
              schema: { type: string, format: binary }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/settings:
    get:
      tags: [Groups]
      summary: Read shared group and membership settings
      description: Requires `GROUP_ADMINISTRATION`, `MEMBER_MANAGEMENT`, `ROLE_MANAGEMENT`, or `FINANCE_MANAGEMENT`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Current typed group settings
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupSettings' }
        '403': { $ref: '#/components/responses/Problem' }
    patch:
      tags: [Groups]
      summary: Partially update group or membership settings
      description: |
        `defaultTheme` requires `GROUP_ADMINISTRATION`. `defaultRoleId` requires `ROLE_MANAGEMENT` or
        `GROUP_ADMINISTRATION`. The deprecated `notificationEmailsEnabled`
        compatibility field requires `GROUP_ADMINISTRATION`. Finance and booking fields require
        `GROUP_ADMINISTRATION` or `FINANCE_MANAGEMENT`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupSettingsUpdate' }
      responses:
        '200':
          description: Persisted typed group settings
          content:
            application/json:
              schema: { $ref: '#/components/schemas/GroupSettings' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/theme-preference:
    put:
      tags: [Groups]
      summary: Update the current member's theme preference
      description: |
        Stores a theme override only on the authenticated account's active
        membership in this group. Null follows the group's current and future
        default theme.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ThemePreference' }
      responses:
        '200':
          description: Persisted membership theme preference
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ThemePreference' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/notification-settings:
    get:
      tags: [Groups]
      summary: Read the group notification event policy
      description: Requires `GROUP_ADMINISTRATION` and returns a strong version ETag.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Versioned event policy and reminder schedule
          headers: { ETag: { schema: { type: string } } }
          content: { application/json: { schema: { $ref: '#/components/schemas/GroupNotificationSettings' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
    put:
      tags: [Groups]
      summary: Replace the group notification event policy
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GroupNotificationSettingsUpdate' }
      responses:
        '200':
          description: Updated policy
          headers: { ETag: { schema: { type: string } } }
          content: { application/json: { schema: { $ref: '#/components/schemas/GroupNotificationSettings' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/notification-preferences:
    get:
      tags: [Groups]
      summary: Read the current membership notification matrix
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Group-enabled events with effective channel availability
          headers: { ETag: { schema: { type: string } } }
          content: { application/json: { schema: { $ref: '#/components/schemas/NotificationPreferences' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
    put:
      tags: [Groups]
      summary: Update independent email and push choices
      description: Disabled group events and unavailable system channels are immutable; retained hidden choices are not deleted.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/NotificationPreferencesUpdate' }
      responses:
        '200':
          description: Updated effective matrix
          headers: { ETag: { schema: { type: string } } }
          content: { application/json: { schema: { $ref: '#/components/schemas/NotificationPreferences' } } }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/transaction-settings:
    get:
      tags: [Groups]
      summary: Read settlement availability and ordered transaction behavior
      description: Returns the feature, form, and member-visible external payment instructions needed by an active group member.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Current operational feature and transaction settings
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TransactionSettings' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/dashboard:
    get:
      tags: [Groups]
      summary: Read the active member dashboard
      description: |
        Returns the authenticated membership's personal account data.
        `groupOutstandingMinor` requires `VIEW_GROUP_STATISTICS`; its omission
        must not be interpreted as a zero group balance. The amount spans the
        complete ledger and is independent of settlement periods.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Dashboard read model
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Dashboard' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/logo:
    post:
      tags: [Groups]
      summary: Validate, normalize, and set the group logo
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [image]
              properties:
                image:
                  type: string
                  format: binary
      responses:
        '200':
          description: Normalized group-logo URL
          content:
            application/json:
              schema:
                type: object
                required: [logoUrl]
                properties:
                  logoUrl: { type: string }
        '403': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Groups]
      summary: Restore the default TeamTaler logo for the group
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Custom group logo removed }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/members:
    get:
      tags: [Groups]
      summary: List group members and effective permissions
      description: |
        Requires `VIEW_MEMBER_DIRECTORY`. `BOOK_FOR_OTHERS` implies that
        compatible read permission, while `MEMBER_MANAGEMENT` implies it and
        additionally exposes archived memberships. Callers that only need valid
        booking targets must use the
        privacy-minimized `/booking-context` read model instead of this
        administrative directory.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Group members
          headers:
            ETag:
              schema: { type: string }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Membership' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/booking-context:
    get:
      tags: [Bookings]
      summary: Read the privacy-minimized context required by the booking workspace
      description: |
        Requires `CREATE_OWN_BOOKING`, `BOOK_FOR_OTHERS`, or `BOOK_FOR_GUESTS`.
        The response never
        includes the group member directory, email addresses, role assignments,
        effective grants for other memberships, or group statistics. Targets
        are filtered independently: own access needs
        `CREATE_OWN_BOOKING`, credentialed members need `BOOK_FOR_OTHERS`, and
        credentialless temporary guests need `BOOK_FOR_GUESTS`. Temporary guests
        sort after credentialed members. `canBookForGuests` also controls inline
        temporary-guest creation.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Minimal current-period, own-balance, and target context
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BookingContext' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/public-join-link:
    get:
      tags: [Groups]
      summary: Read the member-manager-owned public join-link state
      description: |
        Requires `MEMBER_MANAGEMENT`. The reusable acceptance URL is returned
        only while the single group link is enabled and unexpired. Version zero
        represents a group that has never created a link.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Current public join-link state
          headers:
            ETag:
              description: Strong ETag when a persisted link exists.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PublicJoinLink' }
        '403': { $ref: '#/components/responses/Problem' }
    put:
      tags: [Groups]
      summary: Create, enable, disable, or change the lifetime of a public join link
      description: |
        Requires `MEMBER_MANAGEMENT`. The first persisted version omits
        `If-Match`; later replacements require the current strong ETag. Enabling
        a disabled or expired link creates a new token. A null expiry means
        unlimited availability. Enabling requires configured email delivery and
        a non-administrative group default role.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PublicJoinLinkUpdate' }
      responses:
        '200':
          description: Persisted public join-link state
          headers:
            ETag:
              description: Strong ETag derived from the returned version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PublicJoinLink' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/public-join-link/rotate:
    post:
      tags: [Groups]
      summary: Replace the current public join-link token
      description: |
        Requires `MEMBER_MANAGEMENT` and an enabled, unexpired link. Rotation
        invalidates the previous URL, QR code, and all pending registrations in
        the same write transaction while preserving the configured expiry.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '200':
          description: Rotated public join-link state
          headers:
            ETag:
              description: Strong ETag derived from the incremented version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PublicJoinLink' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/roles:
    get:
      tags: [Authorization]
      summary: List group-owned roles and assignment counts
      description: Requires `ROLE_MANAGEMENT` or `MEMBER_MANAGEMENT` in the requested group.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Roles with explicit grants, versions, protection metadata, and assignment counts
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Role' }
        '403': { $ref: '#/components/responses/Problem' }
    post:
      tags: [Authorization]
      summary: Create a group-owned role
      description: |
        Requires `ROLE_MANAGEMENT`. A role containing
        `GROUP_ADMINISTRATION` additionally requires
        `GROUP_ADMINISTRATION`. Only `GROUP` grants are accepted in v1.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RoleCommand' }
      responses:
        '201':
          description: Created role
          headers:
            ETag:
              description: Strong ETag derived from the returned role version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Role' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/roles/{roleId}:
    get:
      tags: [Authorization]
      summary: Read one group-owned role
      description: Requires `ROLE_MANAGEMENT` in the requested group.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/RoleId'
      responses:
        '200':
          description: Current role
          headers:
            ETag:
              description: Strong ETag derived from the returned role version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Role' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
    put:
      tags: [Authorization]
      summary: Replace a role name, description, and explicit grants
      description: |
        Requires `ROLE_MANAGEMENT`. Changing `GROUP_ADMINISTRATION` or the
        reserved administrator role additionally requires
        `GROUP_ADMINISTRATION`. The reserved role name and its direct
        `GROUP_ADMINISTRATION`, `MEMBER_MANAGEMENT`, and `ROLE_MANAGEMENT`
        grants are immutable. Only `GROUP` grants are accepted in v1.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/RoleId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RoleCommand' }
      responses:
        '200':
          description: Updated role
          headers:
            ETag:
              description: Strong ETag derived from the incremented role version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Role' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Authorization]
      summary: Delete an unused editable role
      description: |
        Requires `ROLE_MANAGEMENT` and a matching role version. The reserved
        group-administrator role cannot be deleted; deleting an editable role that contains
        `GROUP_ADMINISTRATION` additionally requires that permission. A role
        assigned to an active membership or pending invitation returns `409`
        with both assignment counts. Expired invitation assignments do not
        block deletion and are removed by cascade.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/RoleId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '204': { description: Role deleted }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/RoleConflictProblem' }
        '412': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/role-assignments:
    get:
      tags: [Authorization]
      summary: List aggregated member and pending-invitation role assignments
      description: Requires `MEMBER_MANAGEMENT` in the requested group.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: One complete role-ID set and version per assignment subject
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/RoleAssignment' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/members/{membershipId}/roles:
    put:
      tags: [Authorization]
      summary: Replace all role assignments for an active membership
      description: |
        At least one role must remain assigned. Every assignment requires
        `MEMBER_MANAGEMENT`. Reserved administrator assignment changes and any
        other role containing `GROUP_ADMINISTRATION` additionally require
        `GROUP_ADMINISTRATION`. The last active reserved administrator
        assignment cannot be removed.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RoleAssignmentUpdate' }
      responses:
        '200':
          description: Replaced membership role assignments
          headers:
            ETag:
              description: Strong ETag derived from the incremented assignment version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RoleAssignment' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/members/{membershipId}/permissions:
    patch:
      tags: [Groups]
      summary: Replace deprecated preset-role and legacy permission fields
      description: |
        Requires `MEMBER_MANAGEMENT`. Deprecated compatibility adapter.
        Preset-role writes preserve custom
        role assignments, `SELF_RECORD_PAYMENT` is mapped through the editable
        migration role, and every non-empty `categoryGrants` value returns
        `422`. Assignment changes use the same authorization policy as the
        dynamic role API and require the current assignment ETag.
      deprecated: true
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PermissionUpdate' }
      responses:
        '204':
          description: Permissions replaced
          headers:
            ETag:
              description: Strong ETag derived from the resulting assignment version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/members/{membershipId}:
    patch:
      tags: [Groups]
      summary: Rename an active temporary guest
      description: |
        Requires `MEMBER_MANAGEMENT`. Only an active credentialless temporary
        guest may be renamed through this endpoint. Credentialed members manage
        their global display name through account flows.
        The normalized display name is written with an audit event. Repeating
        the same replacement is naturally idempotent and needs no
        `Idempotency-Key`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TemporaryGuestRenameCommand' }
      responses:
        '200':
          description: Renamed temporary guest membership
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Membership' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Groups]
      summary: Archive a membership while preserving all historical records
      description: |
        Requires `MEMBER_MANAGEMENT`. The same lifecycle applies to regular
        memberships and temporary guests. Archival clears
        effective access while preserving the stable membership identifier,
        credentialless identity, bookings, ledger entries, period statements,
        claim-invitation history, notifications, and audit events.
        Archiving a membership with a role that grants `GROUP_ADMINISTRATION`
        additionally requires `GROUP_ADMINISTRATION`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
        - $ref: '#/components/parameters/CSRFToken'
        - name: confirmSelf
          in: query
          required: false
          schema: { type: boolean, default: false }
          description: Must be true when a member archives their own membership.
      responses:
        '204': { description: Membership archived and effective permissions cleared }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/members/{membershipId}/reactivate:
    post:
      tags: [Groups]
      summary: Reactivate an archived membership
      description: |
        Requires `MEMBER_MANAGEMENT`. Credentialed memberships must receive at
        least one role. A role granting `GROUP_ADMINISTRATION` additionally
        requires `GROUP_ADMINISTRATION`. Temporary guests remain roleless and
        may provide a conflict-free replacement display name. Permanently deleted
        memberships cannot be reactivated.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/MemberReactivationCommand' }
      responses:
        '200':
          description: Reactivated membership
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Membership' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/members/{membershipId}/permanent:
    delete:
      tags: [Groups]
      summary: Permanently remove an archived membership from administration
      description: |
        Requires `MEMBER_MANAGEMENT` and an exact zero consolidated balance.
        The membership identifier and its last display name remain as a
        credentialless tombstone for bookings, payments, ledger entries,
        statements, and audit history. The original account and memberships in
        other groups remain unchanged. Later financial corrections and booking
        reversals may create a new balance on the deleted tombstone account.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Membership removed from administration; historical tombstone retained }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/members/{membershipId}/claim-invitation:
    post:
      tags: [Groups]
      summary: Invite an active temporary guest to claim login access
      description: |
        Requires `MEMBER_MANAGEMENT`. The command creates one seven-day,
        single-use invitation tied to the active credentialless membership and
        records its email outbox work atomically when SMTP is available. The
        fallback URL is returned once. The command accepts no
        `Idempotency-Key`: at most one open claim invitation may target a guest,
        and a repeated request returns `409` without rotating the token or
        sending another message.

        Acceptance preserves the target membership identifier and every
        accounting and audit reference. A new claimant receives credentials on
        the temporary identity; an existing account must authenticate and is
        linked without creating a second membership. At least one role is stored
        with the invitation and applied exactly on acceptance. Member managers
        may select ordinary roles; roles granting `GROUP_ADMINISTRATION`
        additionally require `GROUP_ADMINISTRATION`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TemporaryGuestClaimInvitationCommand' }
      responses:
        '201':
          description: Claim invitation created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TemporaryGuestClaimInvitationResult' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/invitations:
    get:
      tags: [Groups]
      summary: List invitation metadata without secret tokens
      description: |
        Requires `MEMBER_MANAGEMENT` and returns the complete invitation
        lifecycle without bearer tokens.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Invitations
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Invitation' }
        '403': { $ref: '#/components/responses/Problem' }
    post:
      tags: [Groups]
      summary: Create a seven-day, one-time invitation and optionally queue its email
      description: |
        Requires `MEMBER_MANAGEMENT`.
        When SMTP delivery is configured, the invitation and its encrypted
        transactional outbox job are committed atomically. The one-time
        acceptance URL is returned regardless of SMTP availability so a group
        member manager can use it as a fallback. A current invitation or group
        membership for the normalized email address prevents duplicates across
        manual creation and CSV imports. Selected `roleIds` and deprecated role
        fields apply the same member-management and administrator-capable-role
        checks as the dedicated assignment endpoint. At least one role must be
        selected explicitly.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/InvitationCommand' }
      responses:
        '201':
          description: Invitation with delivery status and one-time fallback acceptance URL
          content:
            application/json:
              schema:
                type: object
                required: [invitation, acceptUrl]
                properties:
                  invitation: { $ref: '#/components/schemas/Invitation' }
                  acceptUrl: { type: string, format: uri }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/invitations/import:
    post:
      tags: [Groups]
      summary: Import invitations with row-specific roles or the group default
      description: |
        Requires `MEMBER_MANAGEMENT`.
        Accepts UTF-8 CSV with a required `email` column and an optional
        `display_name` and `roles` columns. Multiple case-insensitive role names
        in one row are separated by `|`. A blank roles cell uses the deprecated
        shared `roleId` parameters when present, otherwise the group's default
        role. Comma and semicolon separators and an optional UTF-8 BOM are
        supported. Valid rows are committed together with an encrypted
        transactional email-outbox job. A membership is created only after the
        recipient accepts the invitation.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
        - name: roleId
          in: query
          required: false
          deprecated: true
          description: Deprecated shared fallback. Repeat for every group-owned role assigned to rows without a roles value.
          schema: { type: array, uniqueItems: true, items: { type: string } }
          style: form
          explode: true
      requestBody:
        required: true
        content:
          text/csv:
            schema:
              type: string
              maxLength: 262144
              examples:
                - |-
                  email,display_name,roles
                  alex@example.com,Alex Member,Mitglied
                  sam@example.com,Sam Member,Finanzverwaltung|Katalogverwaltung
      responses:
        '200':
          description: Complete or partial row-level import result
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InvitationImportResult' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '415': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/invitations/{invitationId}:
    patch:
      tags: [Groups]
      summary: Replace display-name and compatible role defaults of an open invitation
      description: |
        Requires `MEMBER_MANAGEMENT`.
        Both `roleIds` and deprecated role fields use the current assignment
        policy and require a matching `If-Match`. Legacy role fields preserve
        custom assignments. Non-empty legacy category grants return `422`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/InvitationId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/InvitationUpdate' }
      responses:
        '200':
          description: Updated invitation metadata
          headers:
            ETag:
              description: Strong ETag derived from the current invitation assignment version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invitation' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Groups]
      summary: Revoke an open invitation and cancel unsent email delivery
      description: Requires `MEMBER_MANAGEMENT`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - name: invitationId
          in: path
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/CSRFToken'
      responses:
        '204': { description: Invitation revoked }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/invitations/{invitationId}/roles:
    put:
      tags: [Authorization]
      summary: Replace all role assignments for an ordinary pending invitation
      description: |
        Managed-guest claim invitations have a server-owned guest-role
        assignment and return `409` from this operation.
        At least one role must remain assigned. Every assignment requires
        `MEMBER_MANAGEMENT`. Changing the reserved administrator role or another
        role containing `GROUP_ADMINISTRATION` additionally requires
        `GROUP_ADMINISTRATION`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/InvitationId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RoleAssignmentUpdate' }
      responses:
        '200':
          description: Replaced pending-invitation role assignments
          headers:
            ETag:
              description: Strong ETag derived from the incremented assignment version.
              schema: { type: string, pattern: '^"v[1-9][0-9]*"$' }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RoleAssignment' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/invitations/{invitationId}/email/retry:
    post:
      tags: [Groups]
      summary: Retry a terminally failed invitation email
      description: Requires `MEMBER_MANAGEMENT`.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - name: invitationId
          in: path
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Email returned to the delivery queue
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InvitationEmailRetryResult' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/invitations/{invitationId}/email/resend:
    post:
      tags: [Groups]
      summary: Renew an open invitation
      description: Requires `MEMBER_MANAGEMENT`. Creates a new token and seven-day expiry and always returns its one-time manual fallback URL. Older links become invalid; PENDING and SENDING deliveries are rejected. Active SMTP additionally queues the renewed invitation for email delivery.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - name: invitationId
          in: path
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Renewed invitation with optional queued email delivery
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InvitationEmailResendResult' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
        '503': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/categories:
    get:
      tags: [Catalog]
      summary: List categories with nested products
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Catalog
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Category' }
    post:
      tags: [Catalog]
      summary: Create a category
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CategoryCommand' }
      responses:
        '201':
          description: Created category
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Category' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/catalog/order:
    put:
      tags: [Catalog]
      summary: Replace the category and product display order
      description: Atomically persists the complete catalog order. Every category and every product in its owning category must appear exactly once; products cannot be moved between categories.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CatalogOrderCommand' }
      responses:
        '200':
          description: Reordered catalog
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Category' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/categories/{categoryId}:
    patch:
      tags: [Catalog]
      summary: Update or archive a category
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CategoryId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CategoryUpdate' }
      responses:
        '200':
          description: Updated category
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Category' }
        '412': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Catalog]
      summary: Permanently delete an archived, empty, unused category
      description: The category must already be archived, contain no products, and have no booking or ledger history. Historical categories remain archived instead.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CategoryId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '204': { description: Category permanently deleted }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/categories/{categoryId}/products:
    post:
      tags: [Catalog]
      summary: Create a product in a category
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CategoryId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProductCommand' }
      responses:
        '201':
          description: Created product
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Product' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/products/{productId}:
    patch:
      tags: [Catalog]
      summary: Update or archive a product
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/ProductId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProductUpdate' }
      responses:
        '200':
          description: Updated product
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Product' }
        '412': { $ref: '#/components/responses/Problem' }
    delete:
      tags: [Catalog]
      summary: Permanently remove an archived product from the catalog
      description: The product must already be archived. Unused products are physically deleted; products referenced by bookings retain an internal tombstone so immutable booking, ledger, settlement, and audit history remains intact. Removed products are no longer returned by catalog APIs and cannot be restored through the API.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/ProductId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/RequiredIfMatch'
      responses:
        '204': { description: Product permanently removed from the catalog }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/products/{productId}/image:
    post:
      tags: [Catalog]
      summary: Validate, normalize, and attach a product image
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/ProductId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [image]
              properties:
                image:
                  type: string
                  format: binary
      responses:
        '200':
          description: Normalized image URL
          content:
            application/json:
              schema:
                type: object
                required: [imageUrl]
                properties:
                  imageUrl: { type: string }
        '403': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/images/{imageKey}:
    get:
      tags: [Catalog]
      summary: Read a group-referenced immutable product image or group logo
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - name: imageKey
          in: path
          required: true
          schema:
            type: string
            pattern: '^[0-9a-f]{64}\.png$'
      responses:
        '200':
          description: Normalized PNG image
          headers:
            ETag:
              schema: { type: string }
            Cache-Control:
              schema: { type: string }
          content:
            image/png:
              schema: { type: string, format: binary }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { description: Image not found }
  /groups/{groupId}/activities:
    get:
      tags: [Activities]
      summary: List the unified account activity feed visible to the current member
      description: |
        Combines bookings, payments, their audited reversals, and direct
        receivable adjustments before filtering, sorting, and cursor pagination.
        Every reversed booking or payment remains in the feed with status
        `REVERSED` and receives one separate `REVERSAL` row at the audited
        reversal time. The original and reversal expose symmetric stable links.
        Members always see their own
        account movements and bookings they recorded for somebody else.
        `VIEW_ALL_BOOKING_ACTIVITY` adds all group bookings, while
        `FINANCE_MANAGEMENT` independently adds all group payments and
        adjustments. A reversal always inherits the visibility of its original.

        Amounts use their visible receivable sign: bookings are positive,
        payments are negative, reversals contain the opposite effect of their
        original, and adjustments retain their ledger sign.
        Repeated `kind`, `categoryId`, and `productId` values use OR semantics
        within each field and AND semantics across fields. Authorization scope
        and the complete filter set are bound to the opaque cursor fingerprint.
        `anchorId` pauses all filters except sorting and returns an authorized
        context window with the requested entry as close to the center as list
        boundaries permit. It cannot be combined with `cursor`; any continuation
        uses the returned cursor without `anchorId` or paused filters.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/TableSearch'
        - $ref: '#/components/parameters/TableCursor'
        - name: anchorId
          in: query
          description: Stable activity identity to center in an unfiltered, permission-scoped context window. Cannot be combined with cursor.
          schema: { type: string, maxLength: 500 }
        - $ref: '#/components/parameters/SortDirection'
        - name: kind
          in: query
          description: One or more transaction kinds. Repeat the parameter for multiple values.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 200
            items: { $ref: '#/components/schemas/ActivityKind' }
        - name: targetMembershipId
          in: query
          schema: { type: string }
        - name: categoryId
          in: query
          description: One or more booking category identifiers.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 200
            items: { type: string }
        - name: productId
          in: query
          description: One or more booking product identifiers.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 200
            items: { type: string }
        - name: status
          in: query
          schema: { type: string, enum: [POSTED, REVERSED] }
        - name: occurredFrom
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: occurredTo
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: amountMin
          in: query
          description: Inclusive signed lower amount in minor units.
          schema: { type: integer, format: int64 }
        - name: amountMax
          in: query
          description: Inclusive signed upper amount in minor units.
          schema: { type: integer, format: int64 }
        - name: sort
          in: query
          schema:
            type: string
            enum: [kind, targetName, actorName, detailName, categoryName, occurredAt, amount, status]
            default: occurredAt
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Globally sorted visible activities
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; omitted on the final page.
              schema: { type: string }
            X-Has-More:
              description: Whether another matching page exists.
              schema: { type: boolean }
            X-Page-Limit:
              description: Effective maximum number of rows in this page.
              schema: { type: integer, minimum: 1, maximum: 200 }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ActivityEntry' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { description: Anchor activity does not exist or is not visible to the current member }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/activities/filter-options:
    get:
      tags: [Activities]
      summary: List filter options available in the authorized activity feed
      description: Derives distinct transaction kinds, target identities, categories, and products from the same permission-scoped source set as the unified feed, including historical product tombstones.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Permission-scoped activity filter catalog
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ActivityFilterOptions' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/bookings:
    get:
      tags: [Bookings]
      summary: List activity bookings visible to the current member
      description: |
        Members with `VIEW_ALL_BOOKING_ACTIVITY`, including through the
        `VOID_ANY_BOOKING` implication, see complete group history. Other active
        members see actor- and target-relevant activity. Search, filters, sorting,
        and keyset pagination execute only after this tenant and visibility scope
        is fixed. The JSON body remains the existing booking array.

        Repeated `categoryId` and `productId` parameters apply OR semantics
        within their own field and AND semantics across fields.

        `createdFrom` is inclusive and `createdTo` is exclusive for RFC 3339
        timestamps. A date-only lower bound starts at 00:00 UTC; a date-only
        upper bound includes the complete selected day and is evaluated as the
        next day's exclusive 00:00 UTC bound. `q` searches actor, target, product,
        category, reason, total minor units, and posting status.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/TableSearch'
        - $ref: '#/components/parameters/TableCursor'
        - $ref: '#/components/parameters/SortDirection'
        - name: periodId
          in: query
          schema: { type: string }
        - name: actorMembershipId
          in: query
          schema: { type: string }
        - name: targetMembershipId
          in: query
          schema: { type: string }
        - name: categoryId
          in: query
          description: One or more category identifiers. Repeat the parameter for multiple values.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 200
            items: { type: string }
        - name: productId
          in: query
          description: One or more product identifiers. Repeat the parameter for multiple values.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 200
            items: { type: string }
        - name: status
          in: query
          schema: { type: string, enum: [POSTED, VOIDED] }
        - name: createdFrom
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: createdTo
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: amountMin
          in: query
          description: Inclusive lower booking total in minor units.
          schema: { type: integer, format: int64, minimum: 0 }
        - name: amountMax
          in: query
          description: Inclusive upper booking total in minor units.
          schema: { type: integer, format: int64, minimum: 0 }
        - name: sort
          in: query
          schema:
            type: string
            enum: [createdAt, amount, targetName, actorName, productName, categoryName, status]
            default: createdAt
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Visible booking snapshots
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; omitted on the final page.
              schema: { type: string }
            X-Has-More:
              description: Whether another matching page exists.
              schema: { type: boolean }
            X-Page-Limit:
              description: Effective maximum number of rows in this page.
              schema: { type: integer, minimum: 1, maximum: 200 }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Booking' }
        '422': { $ref: '#/components/responses/Problem' }
    post:
      tags: [Bookings]
      summary: Create an immutable booking and balanced ledger entries
      description: |
        `CREATE_OWN_BOOKING` permits charging the authenticated membership. A
        different credentialed target requires `BOOK_FOR_OTHERS` and a non-empty
        reason. An existing temporary guest requires `BOOK_FOR_GUESTS` and no
        reason. Inline temporary-guest creation is available through the batch
        command.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BookingCommand' }
      responses:
        '201':
          description: Created booking
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Booking' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/bookings/filter-options:
    get:
      tags: [Bookings]
      summary: List member filter options for visible activity
      description: Returns every distinct booking target in the caller's own-or-all authorized activity scope without exposing email, role, or grant data.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Privacy-minimized activity member catalog
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BookingFilterOptions' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/bookings/{bookingId}/void:
    post:
      tags: [Bookings]
      summary: Create an audited permission-gated booking reversal in the open period
      description: |
        `VOID_OWN_BOOKING` covers the current booking actor or charged target.
        Only the actor may omit the reason during the first 30 seconds; later
        actor reversals and target reversals of incoming third-party bookings
        require a reason. A member who is neither actor nor target requires
        `VOID_ANY_BOOKING` and a reason.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/BookingId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason: { type: string, maxLength: 500 }
      responses:
        '200':
          description: Voided booking snapshot
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Booking' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/accounts:
    get:
      tags: [Finance]
      summary: List consolidated member balances with FINANCE_MANAGEMENT
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Active and archived group membership balances
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/AccountSummary' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/accounts/me:
    get:
      tags: [Finance]
      summary: Read the current member account
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Consolidated account
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
  /groups/{groupId}/accounts/me/movements:
    get:
      tags: [Finance]
      summary: Query the current member's complete receivable movement history
      description: |
        This additive endpoint removes the account response's 50-row presentation
        limit without changing that existing response. Search, typed filters,
        sorting, and keyset pagination are evaluated inside the authenticated
        member's group and membership scope. `createdFrom` is inclusive and
        `createdTo` is exclusive for RFC 3339 timestamps. Date-only bounds select
        UTC calendar days; a date-only upper bound advances to the next day's
        00:00 UTC. `q` searches description, signed minor units, and movement type.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/TableSearch'
        - $ref: '#/components/parameters/TableCursor'
        - $ref: '#/components/parameters/SortDirection'
        - name: periodId
          in: query
          schema: { type: string }
        - name: type
          in: query
          schema: { type: string, enum: [BOOKING, PAYMENT, REVERSAL, ADJUSTMENT] }
        - name: createdFrom
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: createdTo
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: amountMin
          in: query
          description: Inclusive lower signed movement amount in minor units.
          schema: { type: integer, format: int64 }
        - name: amountMax
          in: query
          description: Inclusive upper signed movement amount in minor units.
          schema: { type: integer, format: int64 }
        - name: sort
          in: query
          schema: { type: string, enum: [createdAt, amount, description, type], default: createdAt }
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Complete personal movement page
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; omitted on the final page.
              schema: { type: string }
            X-Has-More:
              description: Whether another matching page exists.
              schema: { type: boolean }
            X-Page-Limit:
              description: Effective maximum number of rows in this page.
              schema: { type: integer, minimum: 1, maximum: 200 }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/LedgerEntry' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/accounts/{membershipId}:
    get:
      tags: [Finance]
      summary: Read a member account with FINANCE_MANAGEMENT
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/MembershipId'
      responses:
        '200':
          description: Consolidated account
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Account' }
        '403': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/payments:
    get:
      tags: [Finance]
      summary: List incoming payments
      description: |
        Search, typed filters, sorting, and keyset pagination execute over the
        complete finance-authorized group history. The JSON body remains the
        existing payment array. `receivedFrom` is inclusive and `receivedTo` is
        exclusive for RFC 3339 timestamps. Date-only bounds select UTC calendar
        days; a date-only upper bound advances to the next day's 00:00 UTC. `q`
        searches member name, recording member name, method identifier and label, reference, note,
        amount minor units, and posting status. Every row includes the current
        identity of the membership that recorded it for mixed activity views.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/TableSearch'
        - $ref: '#/components/parameters/TableCursor'
        - $ref: '#/components/parameters/SortDirection'
        - name: membershipId
          in: query
          schema: { type: string }
        - name: method
          in: query
          description: Exact stable payment-method identifier.
          schema: { type: string, maxLength: 120 }
        - name: status
          in: query
          schema: { type: string, enum: [POSTED, REVERSED] }
        - name: receivedFrom
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: receivedTo
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: amountMin
          in: query
          description: Inclusive lower payment amount in minor units.
          schema: { type: integer, format: int64, minimum: 0 }
        - name: amountMax
          in: query
          description: Inclusive upper payment amount in minor units.
          schema: { type: integer, format: int64, minimum: 0 }
        - name: sort
          in: query
          schema: { type: string, enum: [receivedAt, amount, memberName, actorName, method, status], default: receivedAt }
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Payments
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; omitted on the final page.
              schema: { type: string }
            X-Has-More:
              description: Whether another matching page exists.
              schema: { type: boolean }
            X-Page-Limit:
              description: Effective maximum number of rows in this page.
              schema: { type: integer, minimum: 1, maximum: 200 }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Payment' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
    post:
      tags: [Finance]
      summary: Record and FIFO-allocate an incoming payment
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PaymentCommand' }
          multipart/form-data:
            schema:
              type: object
              additionalProperties: false
              required: [command]
              properties:
                command: { $ref: '#/components/schemas/PaymentCommand' }
                attachment:
                  type: string
                  format: binary
                  description: One JPEG, PNG, WebP, or PDF receipt subject to the effective attachment limit.
            encoding:
              command:
                contentType: application/json
      responses:
        '201':
          description: Recorded payment
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Payment' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/bookings/batch:
    post:
      tags: [Bookings]
      summary: Atomically create the same booking for existing members and new temporary guests
      description: |
        Creates one immutable booking and balanced ledger pair per unique active
        target membership in a single transaction. Selecting the current
        membership requires `CREATE_OWN_BOOKING`; selecting another credentialed
        membership requires `BOOK_FOR_OTHERS` and a non-empty shared reason.
        Selecting an existing temporary guest or supplying
        `temporaryGuestDisplayNames` requires `BOOK_FOR_GUESTS` and no reason.

        Each temporary guest name creates a credentialless user, a deliberately
        roleless active membership, and its booking inside the same transaction.
        Existing target IDs and new names together are limited to 100. Active
        temporary-guest names are
        case-insensitively unique within the group. Any invalid, duplicate,
        archived, or
        cross-group target, unsafe guest configuration, accounting conflict, or
        notification failure rejects the complete command, including all new
        identities. A name conflict never merges automatically; its `409`
        Problem Details includes `existingMembershipId` so the caller may
        explicitly select that active guest in a later command.

        The idempotency payload includes the normalized names and all booking
        fields. A replay returns the original bookings and guest membership IDs;
        reuse of the key with any different payload returns `409`. Responses are
        ordered by `targetMembershipIds` first and then by
        `temporaryGuestDisplayNames`, preserving each request array's order.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BookingBatchCommand' }
      responses:
        '201':
          description: Created booking snapshots in target request order
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Booking' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/bookings/bulk:
    post:
      tags: [Bookings]
      summary: Atomically create multiple product bookings for the same targets
      description: |
        Creates every requested product and target combination, including its
        balanced ledger pair, notification, and audit event, in one transaction.
        The command accepts between 1 and 25 distinct product lines, between 1
        and 100 existing or newly created targets, and expands to at most 500
        immutable bookings. Duplicate product IDs are rejected; callers express
        repeated units through each line's quantity.

        Selecting the current membership requires `CREATE_OWN_BOOKING`;
        selecting another credentialed membership requires `BOOK_FOR_OTHERS`
        and the group's configured shared reason; selecting an existing or new
        temporary guest requires `BOOK_FOR_GUESTS`. New temporary guests are
        created once and reused for every product line in the command.

        For a new idempotency key, every target, permission, product version,
        price, quantity, and the open accounting period are revalidated inside
        the write transaction. Any
        invalid input, concurrent catalog change, guest-name conflict, accounting
        conflict, notification failure, or audit failure rolls back the complete
        command. The idempotency payload covers the normalized targets, names,
        reason, period, and ordered product lines. Reusing a key with a different
        payload returns `409`; replaying the same payload returns the original
        result. Responses are ordered by product line first and then by existing
        target IDs followed by new temporary guests, preserving request order.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BookingBulkCommand' }
      responses:
        '201':
          description: Created booking snapshots in product-line and target request order
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Booking' }
        '403': { $ref: '#/components/responses/Problem' }
        '404': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '412': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/payments/self:
    post:
      tags: [Finance]
      summary: Record and FIFO-allocate a payment for the authenticated membership
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SelfPaymentCommand' }
          multipart/form-data:
            schema:
              type: object
              additionalProperties: false
              required: [command]
              properties:
                command: { $ref: '#/components/schemas/SelfPaymentCommand' }
                attachment:
                  type: string
                  format: binary
                  description: One JPEG, PNG, WebP, or PDF receipt subject to the effective attachment limit.
            encoding:
              command:
                contentType: application/json
      responses:
        '201':
          description: Recorded self-service payment
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Payment' }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
        '413': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/payments/{paymentId}/attachment:
    get:
      tags: [Finance]
      summary: Download a protected immutable payment receipt
      description: |
        Available only to the payment target or a currently effective
        FINANCE_MANAGEMENT holder in the same active group. Missing receipts,
        foreign-group payments, and unauthorized requests all return 404.
        Images are returned inline; PDFs use attachment disposition.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/PaymentId'
      responses:
        '200':
          description: Validated receipt bytes
          headers:
            Cache-Control:
              schema: { type: string, example: 'private, no-store' }
            Content-Disposition:
              schema: { type: string }
            X-Content-Type-Options:
              schema: { type: string, example: nosniff }
          content:
            image/jpeg: { schema: { type: string, format: binary } }
            image/png: { schema: { type: string, format: binary } }
            application/pdf: { schema: { type: string, format: binary } }
        '404': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/payments/{paymentId}/reverse:
    post:
      tags: [Finance]
      summary: Reverse a payment and rebuild FIFO allocations
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/PaymentId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, minLength: 1, maxLength: 500 }
      responses:
        '204': { description: Payment reversed }
        '409': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/periods:
    get:
      tags: [Periods]
      summary: List accounting periods
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Newest-first periods
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Period' }
  /groups/{groupId}/periods/{periodId}/close:
    post:
      tags: [Periods]
      summary: Permanently close a period and open its successor
      description: Requires FINANCE_MANAGEMENT and settlementsEnabled=true. Disabling settlements never changes the technical open period.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/PeriodId'
        - $ref: '#/components/parameters/CSRFToken'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ClosePeriodCommand' }
      responses:
        '200':
          description: Closed period, successor, and snapshot count
          content:
            application/json:
              schema:
                type: object
                required: [closedPeriod, openPeriod, statementCount]
                properties:
                  closedPeriod: { $ref: '#/components/schemas/Period' }
                  openPeriod: { $ref: '#/components/schemas/Period' }
                  statementCount: { type: integer, format: int64 }
        '403': { $ref: '#/components/responses/Problem' }
        '409': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/periods/{periodId}/statements:
    get:
      tags: [Periods]
      summary: Read statements for one closed period
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/PeriodId'
      responses:
        '200':
          description: Visible statements
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Statement' }
  /groups/{groupId}/settlements:
    get:
      tags: [Periods]
      summary: List visible settlement statements
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - name: periodId
          in: query
          schema: { type: string }
      responses:
        '200':
          description: Settlement statements
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Statement' }
  /groups/{groupId}/notifications:
    get:
      tags: [Groups]
      summary: List current member notifications
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/Limit'
        - name: cursor
          in: query
          description: Opaque notification identifier returned in the preceding X-Next-Cursor header.
          schema: { type: string }
      responses:
        '200':
          description: Notifications
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next older page; omitted for the final page.
              schema: { type: string }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Notification' }
  /groups/{groupId}/notifications/summary:
    get:
      tags: [Groups]
      summary: Read the current member's exact unread notification count
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Unread notification summary
          content:
            application/json:
              schema: { $ref: '#/components/schemas/NotificationSummary' }
  /groups/{groupId}/notifications/read:
    patch:
      tags: [Groups]
      summary: Acknowledge visible notifications in one bounded batch
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [notificationIds]
              properties:
                notificationIds:
                  type: array
                  minItems: 1
                  maxItems: 100
                  uniqueItems: true
                  items: { type: string, minLength: 1 }
      responses:
        '200':
          description: Batch acknowledgement result
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/NotificationSummary'
                  - type: object
                    required: [readAt]
                    properties:
                      readAt: { type: string, format: date-time }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/notifications/{notificationId}:
    patch:
      tags: [Groups]
      summary: Mark one notification read or unread
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - name: notificationId
          in: path
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/CSRFToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [read]
              properties:
                read: { type: boolean }
      responses:
        '200':
          description: Updated notification
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Notification' }
  /groups/{groupId}/audit:
    get:
      tags: [Groups]
      summary: List append-only administrative audit events
      description: |
        Search, typed filters, sorting, and keyset pagination execute inside the
        authorized group only. The JSON body remains the existing event array.
        `occurredFrom` is inclusive and `occurredTo` is exclusive for RFC 3339
        timestamps. Date-only bounds select UTC calendar days; a date-only upper
        bound advances to the next day's 00:00 UTC. `q` searches actor name,
        action, resource type, resource identifier, and serialized safe metadata.
      parameters:
        - $ref: '#/components/parameters/GroupId'
        - $ref: '#/components/parameters/TableSearch'
        - $ref: '#/components/parameters/TableCursor'
        - $ref: '#/components/parameters/SortDirection'
        - name: actorUserId
          in: query
          schema: { type: string }
        - name: actorMembershipId
          in: query
          schema: { type: string }
        - name: action
          in: query
          description: Exact action values combined with OR semantics when repeated.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 100
            items: { type: string, maxLength: 200 }
        - name: resourceType
          in: query
          description: Exact resource-type values combined with OR semantics when repeated.
          style: form
          explode: true
          schema:
            type: array
            maxItems: 100
            items: { type: string, maxLength: 200 }
        - name: occurredFrom
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: occurredTo
          in: query
          schema:
            oneOf:
              - { type: string, format: date }
              - { type: string, format: date-time }
        - name: sort
          in: query
          schema: { type: string, enum: [occurredAt, actorName, action, resourceType], default: occurredAt }
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Audit events
          headers:
            X-Next-Cursor:
              description: Opaque cursor for the next page; omitted on the final page.
              schema: { type: string }
            X-Has-More:
              description: Whether another matching page exists.
              schema: { type: boolean }
            X-Page-Limit:
              description: Effective maximum number of rows in this page.
              schema: { type: integer, minimum: 1, maximum: 200 }
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/AuditEvent' }
        '403': { $ref: '#/components/responses/Problem' }
        '422': { $ref: '#/components/responses/Problem' }
  /groups/{groupId}/audit/filter-options:
    get:
      tags: [Groups]
      summary: List group-audit filter options
      description: Returns every distinct membership actor, action, and resource type currently present in the authorized group audit log. Newly recorded values appear automatically.
      parameters:
        - $ref: '#/components/parameters/GroupId'
      responses:
        '200':
          description: Complete data-derived group-audit filter catalog
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AuditFilterOptions' }
        '401': { $ref: '#/components/responses/Problem' }
        '403': { $ref: '#/components/responses/Problem' }
  /health/live:
    get:
      tags: [Operations]
      summary: Process liveness probe
      security: []
      servers:
        - url: /
      responses:
        '200': { description: Process is live }
  /health/ready:
    get:
      tags: [Operations]
      summary: Database readiness probe
      security: []
      servers:
        - url: /
      responses:
        '200': { description: Database is ready }
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: teamtaler_session
  parameters:
    GroupId:
      name: groupId
      in: path
      required: true
      schema: { type: string }
    UserId:
      name: userId
      in: path
      required: true
      schema: { type: string }
    MembershipId:
      name: membershipId
      in: path
      required: true
      schema: { type: string }
    RoleId:
      name: roleId
      in: path
      required: true
      schema: { type: string }
    InvitationId:
      name: invitationId
      in: path
      required: true
      schema: { type: string }
    CategoryId:
      name: categoryId
      in: path
      required: true
      schema: { type: string }
    ProductId:
      name: productId
      in: path
      required: true
      schema: { type: string }
    BookingId:
      name: bookingId
      in: path
      required: true
      schema: { type: string }
    PaymentId:
      name: paymentId
      in: path
      required: true
      schema: { type: string }
    PeriodId:
      name: periodId
      in: path
      required: true
      schema: { type: string }
    ExportId:
      name: exportId
      in: path
      required: true
      schema: { type: string, minLength: 1 }
    CSRFToken:
      name: X-CSRF-Token
      in: header
      required: true
      schema: { type: string, minLength: 1 }
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema: { type: string, minLength: 8, maxLength: 200 }
    IfMatch:
      name: If-Match
      in: header
      required: false
      description: Optional strong resource ETag; conditionally required by commands that include versioned role assignments.
      schema: { type: string }
    RequiredIfMatch:
      name: If-Match
      in: header
      required: true
      description: Strong entity tag containing the current resource version, for example `"v3"`.
      schema:
        type: string
        pattern: '^"v[1-9][0-9]*"$'
    Limit:
      name: limit
      in: query
      schema: { type: integer, minimum: 1, maximum: 200, default: 100 }
    TableSearch:
      name: q
      in: query
      description: Case-insensitive global contains search across the table's documented display fields.
      schema: { type: string, maxLength: 200 }
    TableCursor:
      name: cursor
      in: query
      description: Opaque continuation token from X-Next-Cursor. It is valid only with the same filters, sort, direction, and authorization scope.
      schema: { type: string, maxLength: 2048 }
    SortDirection:
      name: direction
      in: query
      description: Sort direction; descending is the compatibility default.
      schema: { type: string, enum: [asc, desc], default: desc }
  responses:
    Problem:
      description: RFC 9457 problem details
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/Problem' }
    RoleConflictProblem:
      description: RFC 9457 conflict details including current role assignment counts
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/RoleConflictProblem' }
  schemas:
    MinorUnits:
      type: string
      pattern: '^-?[0-9]+$'
      description: Exact signed integer amount in the currency's minor unit, encoded as a decimal string to preserve 64-bit precision in browsers.
      examples: ['1250']
    TableExportFormat:
      type: string
      enum: [CSV, PDF]
    GroupTableExportId:
      type: string
      enum:
        - ACTIVITIES
        - PAYMENTS
        - ACCOUNT_BALANCES
        - GROUP_SETTLEMENTS
        - SETTLEMENT_STATEMENT
        - PERSONAL_SETTLEMENTS
        - ACTIVE_MEMBERS
        - ARCHIVED_MEMBERS
        - GROUP_AUDIT
    SystemTableExportId:
      type: string
      enum: [SYSTEM_AUDIT]
    GroupTableExportCommand:
      type: object
      additionalProperties: false
      required: [table, format, timeZone, query]
      properties:
        table: { $ref: '#/components/schemas/GroupTableExportId' }
        format: { $ref: '#/components/schemas/TableExportFormat' }
        timeZone:
          type: string
          minLength: 1
          maxLength: 64
          description: IANA time-zone name used only for localized visible timestamps.
          examples: [Europe/Berlin]
        query:
          description: The selected table's existing typed filter and sort object, without cursor or limit.
          anyOf:
            - $ref: '#/components/schemas/ActivityTableExportQuery'
            - $ref: '#/components/schemas/PaymentTableExportQuery'
            - $ref: '#/components/schemas/AccountBalanceTableExportQuery'
            - $ref: '#/components/schemas/GroupSettlementTableExportQuery'
            - $ref: '#/components/schemas/SettlementStatementTableExportQuery'
            - $ref: '#/components/schemas/PersonalSettlementTableExportQuery'
            - $ref: '#/components/schemas/MemberTableExportQuery'
            - $ref: '#/components/schemas/GroupAuditTableExportQuery'
    SystemTableExportCommand:
      type: object
      additionalProperties: false
      required: [table, format, timeZone, query]
      properties:
        table: { $ref: '#/components/schemas/SystemTableExportId' }
        format: { $ref: '#/components/schemas/TableExportFormat' }
        timeZone:
          type: string
          minLength: 1
          maxLength: 64
          description: IANA time-zone name used only for localized visible timestamps.
          examples: [Europe/Berlin]
        query:
          description: The system-audit table's existing typed filter and sort object, without cursor or limit.
          $ref: '#/components/schemas/SystemAuditTableExportQuery'
    ActivityTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        q: { type: string, maxLength: 200 }
        sort: { type: string, enum: [kind, targetName, actorName, detailName, categoryName, occurredAt, amount, status] }
        direction: { type: string, enum: [asc, desc] }
        kind:
          type: array
          maxItems: 200
          uniqueItems: true
          items: { type: string, enum: [BOOKING, PAYMENT, ADJUSTMENT] }
        targetMembershipId: { type: string }
        categoryId:
          type: array
          maxItems: 200
          uniqueItems: true
          items: { type: string }
        productId:
          type: array
          maxItems: 200
          uniqueItems: true
          items: { type: string }
        status: { type: string, enum: [POSTED, REVERSED] }
        occurredFrom:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        occurredTo:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        amountMin: { $ref: '#/components/schemas/MinorUnits' }
        amountMax: { $ref: '#/components/schemas/MinorUnits' }
    PaymentTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        q: { type: string, maxLength: 200 }
        sort: { type: string, enum: [receivedAt, amount, memberName, actorName, method, status] }
        direction: { type: string, enum: [asc, desc] }
        membershipId: { type: string }
        method: { type: string }
        status: { type: string, enum: [POSTED, REVERSED] }
        receivedFrom:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        receivedTo:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        amountMin: { $ref: '#/components/schemas/MinorUnits' }
        amountMax: { $ref: '#/components/schemas/MinorUnits' }
    AccountBalanceTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        q: { type: string, maxLength: 200 }
        sort: { type: string, enum: [memberName, membershipStatus, balanceState, amount] }
        direction: { type: string, enum: [asc, desc] }
        membershipId: { type: string }
        membershipStatus:
          type: array
          uniqueItems: true
          items: { type: string, enum: [ACTIVE, ARCHIVED, DELETED] }
        balanceState:
          type: array
          uniqueItems: true
          items: { type: string, enum: [due, settled, credit] }
        amountMin: { $ref: '#/components/schemas/MinorUnits' }
        amountMax: { $ref: '#/components/schemas/MinorUnits' }
    GroupSettlementTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        q: { type: string, maxLength: 200 }
        sort: { type: string, enum: [periodLabel, dueAt, memberName, amount, paidAmount, status] }
        direction: { type: string, enum: [asc, desc] }
        membershipId: { type: string }
        periodId: { type: string }
        dueFrom: { type: string, format: date }
        dueTo: { type: string, format: date }
        amountMin: { $ref: '#/components/schemas/MinorUnits' }
        amountMax: { $ref: '#/components/schemas/MinorUnits' }
        status:
          type: array
          uniqueItems: true
          items: { type: string, enum: [OPEN, PARTIAL, PAID, CREDIT] }
    SettlementStatementTableExportQuery:
      type: object
      additionalProperties: false
      required: [membershipId, periodId]
      properties:
        membershipId:
          type: string
          minLength: 1
          description: Exact statement membership identifier in the active group.
        periodId:
          type: string
          minLength: 1
          description: Exact closed accounting-period identifier in the active group.
    PersonalSettlementTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        q: { type: string, maxLength: 200 }
        sort: { type: string, enum: [periodLabel, dueAt, amount, paidAmount, openAmount, status] }
        direction: { type: string, enum: [asc, desc] }
        periodId: { type: string }
        dueFrom: { type: string, format: date }
        dueTo: { type: string, format: date }
        status:
          type: array
          uniqueItems: true
          items: { type: string, enum: [OPEN, PARTIAL, PAID, CREDIT] }
    MemberTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        sort: { type: string, enum: [displayName, email, roles] }
        direction: { type: string, enum: [asc, desc] }
    GroupAuditTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        q: { type: string, maxLength: 200 }
        actorUserId: { type: string }
        actorMembershipId: { type: string }
        action:
          type: array
          maxItems: 100
          uniqueItems: true
          items: { type: string, maxLength: 200 }
        resourceType:
          type: array
          maxItems: 100
          uniqueItems: true
          items: { type: string, maxLength: 200 }
        occurredFrom:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        occurredTo:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        sort: { type: string, enum: [occurredAt, actorName, action, resourceType] }
        direction: { type: string, enum: [asc, desc] }
    SystemAuditTableExportQuery:
      type: object
      additionalProperties: false
      properties:
        q: { type: string, maxLength: 200 }
        actorUserId: { type: string }
        action:
          type: array
          maxItems: 100
          uniqueItems: true
          items: { type: string, maxLength: 200 }
        resourceType:
          type: array
          maxItems: 100
          uniqueItems: true
          items: { type: string, maxLength: 200 }
        occurredFrom:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        occurredTo:
          oneOf:
            - { type: string, format: date }
            - { type: string, format: date-time }
        sort: { type: string, enum: [occurredAt, actorName, action, resourceType] }
        direction: { type: string, enum: [asc, desc] }
    DataExportCreateCommand:
      type: object
      additionalProperties: false
      required: [currentPassword]
      properties:
        currentPassword:
          type: string
          format: password
          minLength: 1
          maxLength: 1024
          description: Consumed for this reauthentication only; never persisted or included in an idempotency fingerprint.
    DataExportScope:
      type: string
      enum: [GROUP, PERSONAL]
    DataExportStatus:
      type: string
      enum: [QUEUED, RUNNING, READY, FAILED, CANCELLED, EXPIRED]
    DataExportProgress:
      type: object
      additionalProperties: false
      required: [completed, total]
      properties:
        completed: { type: integer, minimum: 0 }
        total: { type: integer, minimum: 0 }
    DataExportJob:
      type: object
      description: |
        Actor-owned asynchronous archive state. One generation attempt is
        bounded to 10 minutes and a complete artifact is bounded to 512 MiB.
        `READY` artifacts expire 24 hours after completion.
      additionalProperties: false
      required: [id, scope, status, requestedAt]
      properties:
        id: { type: string }
        scope: { $ref: '#/components/schemas/DataExportScope' }
        status: { $ref: '#/components/schemas/DataExportStatus' }
        requestedAt: { type: string, format: date-time }
        startedAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time }
        expiresAt:
          type: string
          format: date-time
          description: Present for a ready artifact and fixed at 24 hours after completion.
        sizeBytes:
          type: string
          pattern: '^[0-9]+$'
          description: Exact complete ZIP size as a decimal string.
        sha256:
          type: string
          pattern: '^[a-f0-9]{64}$'
          description: Lowercase hexadecimal SHA-256 digest of the complete ZIP artifact.
        progress: { $ref: '#/components/schemas/DataExportProgress' }
        downloadUrl:
          type: string
          format: uri-reference
          description: Actor-bound relative URL, present only while the job is ready and unexpired.
        errorCode:
          type: string
          description: Safe machine-readable failure code with no provider, SQL, path, or secret detail.
    LoginCommand:
      type: object
      additionalProperties: false
      required: [email, password]
      properties:
        email: { type: string, format: email, maxLength: 254 }
        password: { type: string, format: password, minLength: 12, maxLength: 1024 }
    AuthenticationCapabilities:
      type: object
      additionalProperties: false
      required: [passwordResetAvailable, emailChangeAvailable]
      properties:
        passwordResetAvailable:
          type: boolean
          readOnly: true
          description: Whether password-reset email delivery is currently configured.
        emailChangeAvailable:
          type: boolean
          readOnly: true
          description: Whether verified email-address changes are currently configured.
    InstanceCapabilities:
      type: object
      additionalProperties: false
      required: [instanceName, maintenanceMode, publicJoinEnabled, mediaUploadMaxBytes, attachmentUploadMaxBytes, emailNotificationsAvailable, webPushAvailable]
      properties:
        instanceName:
          type: string
          minLength: 1
          maxLength: 120
        maintenanceMode:
          type: boolean
        maintenanceMessage:
          type: string
          maxLength: 240
        publicJoinEnabled:
          type: boolean
          description: Effective global policy; a group link may impose additional restrictions.
        mediaUploadMaxBytes:
          type: integer
          format: int64
          minimum: 1048576
          maximum: 26214400
        attachmentUploadMaxBytes:
          type: integer
          format: int64
          minimum: 1048576
          maximum: 52428800
          description: Effective maximum raw size of one payment receipt.
        emailNotificationsAvailable: { type: boolean }
        webPushAvailable: { type: boolean }
        webPushPublicKey: { type: string }
        webPushKeyId: { type: string }
    PushSubscriptionDevice:
      type: object
      additionalProperties: false
      required: [id, deviceLabel, keyId, current, createdAt, lastUsedAt]
      properties:
        id: { type: string }
        deviceLabel: { type: string, minLength: 1, maxLength: 80 }
        keyId: { type: string }
        current: { type: boolean, description: Whether this device uses the current VAPID identity. }
        createdAt: { type: string, format: date-time }
        lastUsedAt: { type: string, format: date-time }
    PushSubscriptionRegistration:
      type: object
      additionalProperties: false
      required: [label, keyId, subscription]
      properties:
        label: { type: string, minLength: 1, maxLength: 80 }
        keyId: { type: string }
        subscription:
          type: object
          additionalProperties: false
          required: [endpoint, keys]
          properties:
            endpoint: { type: string, format: uri, maxLength: 2048 }
            expirationTime: { type: [integer, 'null'], format: int64 }
            keys:
              type: object
              additionalProperties: false
              required: [auth, p256dh]
              properties:
                auth: { type: string, minLength: 1, maxLength: 128, writeOnly: true }
                p256dh: { type: string, minLength: 1, maxLength: 256, writeOnly: true }
    SystemRole:
      type: string
      enum: [SYSTEM_ADMINISTRATOR]
    SystemSettingSource:
      type: string
      enum: [DATABASE, ENVIRONMENT, CODE]
    EffectiveStringSetting:
      type: object
      additionalProperties: false
      required: [value, source]
      properties:
        value: { type: string }
        source: { $ref: '#/components/schemas/SystemSettingSource' }
        overrideVersion:
          type: integer
          format: int64
          minimum: 1
          description: Present only when the value comes from a persisted database override.
        updatedAt:
          type: string
          format: date-time
          description: Present only when a persisted override supplies the value.
    EffectiveBooleanSetting:
      type: object
      additionalProperties: false
      required: [value, source]
      properties:
        value: { type: boolean }
        source: { $ref: '#/components/schemas/SystemSettingSource' }
        overrideVersion:
          type: integer
          format: int64
          minimum: 1
          description: Present only when the value comes from a persisted database override.
        updatedAt:
          type: string
          format: date-time
          description: Present only when a persisted override supplies the value.
    EffectiveIntegerSetting:
      type: object
      additionalProperties: false
      required: [value, source]
      properties:
        value: { type: integer, format: int64 }
        source: { $ref: '#/components/schemas/SystemSettingSource' }
        overrideVersion:
          type: integer
          format: int64
          minimum: 1
          description: Present only when the value comes from a persisted database override.
        updatedAt:
          type: string
          format: date-time
          description: Present only when a persisted override supplies the value.
    SystemSecretSetting:
      type: object
      additionalProperties: false
      required: [configured, source]
      properties:
        configured: { type: boolean }
        source: { $ref: '#/components/schemas/SystemSettingSource' }
        overrideVersion: { type: integer, format: int64, minimum: 1 }
        updatedAt: { type: string, format: date-time }
    SystemSMTPConfiguration:
      type: object
      additionalProperties: false
      required: [enabled, host, port, tlsMode, username, password, fromAddress, fromName, revision, testStatus, requiresTest, configurationValid, active]
      properties:
        enabled: { $ref: '#/components/schemas/EffectiveBooleanSetting' }
        host: { $ref: '#/components/schemas/EffectiveStringSetting' }
        port:
          allOf:
            - $ref: '#/components/schemas/EffectiveIntegerSetting'
          description: Effective SMTP submission port. The code default is `587`.
        tlsMode:
          allOf:
            - $ref: '#/components/schemas/EffectiveStringSetting'
          description: Effective value is `starttls` or `tls`; plaintext SMTP is unsupported.
        username: { $ref: '#/components/schemas/EffectiveStringSetting' }
        fromAddress: { $ref: '#/components/schemas/EffectiveStringSetting' }
        fromName: { $ref: '#/components/schemas/EffectiveStringSetting' }
        password:
          $ref: '#/components/schemas/SystemSecretSetting'
        revision:
          type: integer
          format: int64
          minimum: 0
        testedRevision:
          type: integer
          format: int64
          minimum: 1
        testedAt:
          type: string
          format: date-time
        testStatus:
          type: string
          enum: [UNTESTED, VERIFIED, FAILED]
          description: Persisted result for the current database-backed SMTP revision.
        requiresTest:
          type: boolean
          description: True when persisted SMTP connection settings must be tested before activation.
        configurationValid:
          type: boolean
          description: Whether the redacted effective fields form a complete TLS-secured configuration.
        active:
          type: boolean
          description: True only when enabled, complete, and trusted or tested at the current SMTP revision.
    SystemWebPushConfiguration:
      type: object
      additionalProperties: false
      required: [enabled, subject, vapidPrivateKey, revision, storageKeyConfigured, configurationValid, active]
      properties:
        enabled: { $ref: '#/components/schemas/EffectiveBooleanSetting' }
        subject: { $ref: '#/components/schemas/EffectiveStringSetting' }
        vapidPrivateKey: { $ref: '#/components/schemas/SystemSecretSetting' }
        publicKey: { type: string, description: URL-safe VAPID public key derived whenever the effective private key is valid. }
        keyId: { type: string, description: SHA-256 public-key identifier used for browser reconciliation. }
        revision: { type: integer, format: int64, minimum: 0 }
        storageKeyConfigured: { type: boolean }
        configurationValid: { type: boolean }
        active: { type: boolean }
    SystemSettings:
      type: object
      additionalProperties: false
      required: [revision, instanceName, defaultCurrency, mediaUploadMaxBytes, mediaUploadHardLimitBytes, attachmentUploadMaxBytes, attachmentUploadHardLimitBytes, publicJoinEnabled, maintenanceMode, maintenanceMessage, smtp, webPush, updatedAt]
      properties:
        revision:
          type: integer
          format: int64
          minimum: 1
        instanceName: { $ref: '#/components/schemas/EffectiveStringSetting' }
        defaultCurrency: { $ref: '#/components/schemas/EffectiveStringSetting' }
        mediaUploadMaxBytes: { $ref: '#/components/schemas/EffectiveIntegerSetting' }
        mediaUploadHardLimitBytes:
          type: integer
          format: int64
          readOnly: true
          description: Compiled application safety ceiling for the runtime-editable media upload limit.
        attachmentUploadMaxBytes: { $ref: '#/components/schemas/EffectiveIntegerSetting' }
        attachmentUploadHardLimitBytes:
          type: integer
          format: int64
          readOnly: true
          description: Compiled 50-MiB ceiling for the runtime-editable payment receipt limit.
        publicJoinEnabled: { $ref: '#/components/schemas/EffectiveBooleanSetting' }
        maintenanceMode: { $ref: '#/components/schemas/EffectiveBooleanSetting' }
        maintenanceMessage: { $ref: '#/components/schemas/EffectiveStringSetting' }
        smtp: { $ref: '#/components/schemas/SystemSMTPConfiguration' }
        webPush: { $ref: '#/components/schemas/SystemWebPushConfiguration' }
        updatedAt: { type: string, format: date-time }
        updatedByUserId: { type: string }
    SystemSettingsUpdate:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        instanceName: { type: string, minLength: 1, maxLength: 120 }
        defaultCurrency: { type: string, pattern: '^[A-Z]{3}$' }
        mediaUploadMaxBytes: { type: integer, format: int64, minimum: 1048576, maximum: 26214400, multipleOf: 1048576 }
        attachmentUploadMaxBytes: { type: integer, format: int64, minimum: 1048576, maximum: 52428800, multipleOf: 1048576 }
        publicJoinEnabled: { type: boolean }
        maintenanceMode: { type: boolean }
        maintenanceMessage: { type: string, maxLength: 240 }
    SystemSettingKey:
      type: string
      enum:
        - instance.name
        - instance.default_currency
        - media.upload_max_bytes
        - attachment.upload_max_bytes
        - access.public_join_enabled
        - maintenance.enabled
        - maintenance.message
        - smtp.enabled
        - smtp.host
        - smtp.port
        - smtp.tls_mode
        - smtp.username
        - smtp.from_address
        - smtp.from_name
        - smtp.password
        - web_push.enabled
        - web_push.subject
        - web_push.vapid_private_key
    SystemSettingsReset:
      type: object
      additionalProperties: false
      required: [keys]
      properties:
        keys:
          type: array
          minItems: 1
          uniqueItems: true
          items: { $ref: '#/components/schemas/SystemSettingKey' }
    SystemSMTPUpdate:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        enabled:
          type: boolean
          description: True is accepted only when the otherwise unchanged current persisted revision is verified.
        host: { type: string, minLength: 1, maxLength: 253 }
        port: { type: integer, minimum: 1, maximum: 65535 }
        tlsMode: { type: string, enum: [starttls, tls] }
        username: { type: string, minLength: 1, maxLength: 254 }
        password:
          type: string
          format: password
          minLength: 1
          maxLength: 1024
          writeOnly: true
          description: Omit only to retain an already persisted password; it is never returned.
        fromAddress: { type: string, format: email, maxLength: 254 }
        fromName: { type: string, maxLength: 120 }
    SystemWebPushUpdate:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        enabled: { type: boolean }
        subject:
          type: string
          maxLength: 512
          description: VAPID contact as an absolute HTTPS URL or `mailto:` URI.
        vapidPrivateKey:
          type: string
          minLength: 43
          maxLength: 43
          pattern: '^[A-Za-z0-9_-]{43}$'
          writeOnly: true
          description: Unpadded base64url P-256 scalar; never returned.
        confirmRotation:
          type: boolean
          default: false
          description: Required when replacing an existing VAPID private key.
    SystemAdministrator:
      type: object
      additionalProperties: false
      required: [userId, email, displayName, active, role, grantedAt]
      properties:
        userId: { type: string }
        email: { type: string, format: email }
        displayName: { type: string }
        active: { type: boolean }
        role: { $ref: '#/components/schemas/SystemRole' }
        grantedAt: { type: string, format: date-time }
        grantedByUserId: { type: string }
    SystemAccount:
      type: object
      additionalProperties: false
      required: [id, email, displayName, active]
      properties:
        id: { type: string }
        email: { type: string, format: email }
        displayName: { type: string }
        active: { type: boolean }
    GroupLifecycleStatus:
      type: string
      enum: [PROVISIONING, ACTIVE, ARCHIVED]
      description: "`PURGING` is an internal transaction state and is never a usable API representation."
    SystemGroup:
      type: object
      additionalProperties: false
      required: [id, name, currency, status, version, createdAt, memberCount, invitationCount, bookingCount, financialRecordCount, auditEventCount, mediaCount]
      properties:
        id: { type: string }
        name: { type: string, minLength: 1, maxLength: 120 }
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        status: { $ref: '#/components/schemas/GroupLifecycleStatus' }
        version: { type: integer, format: int64, minimum: 1 }
        archivedAt: { type: string, format: date-time }
        archivedByUserId: { type: string }
        administratorEmail: { type: string, format: email }
        createdAt: { type: string, format: date-time }
        logoUrl: { type: string }
        memberCount: { type: integer, format: int64, minimum: 0 }
        invitationCount: { type: integer, format: int64, minimum: 0 }
        bookingCount: { type: integer, format: int64, minimum: 0 }
        financialRecordCount: { type: integer, format: int64, minimum: 0 }
        auditEventCount: { type: integer, format: int64, minimum: 0 }
        mediaCount: { type: integer, format: int64, minimum: 0 }
    SystemGroupCreate:
      type: object
      additionalProperties: false
      required: [name, initialAdministratorEmail]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        currency:
          type: string
          pattern: '^[A-Z]{3}$'
          description: Omit to use the current effective new-group default.
        initialAdministratorEmail: { type: string, format: email, maxLength: 254 }
    SystemGroupInvitationResult:
      type: object
      additionalProperties: false
      required: [group]
      properties:
        group: { $ref: '#/components/schemas/SystemGroup' }
        acceptUrl:
          type: string
          format: uri
          description: Present only for a newly created or replaced provisioning invitation. The token remains in the URL fragment and is returned once.
        emailDeliveryStatus:
          $ref: '#/components/schemas/EmailDeliveryStatus'
        expiresAt:
          type: string
          format: date-time
          description: Present with a newly created or replaced provisioning invitation.
    GroupDeletionImpact:
      type: object
      additionalProperties: false
      required: [groupId, groupName, currency, version, memberCount, openBalanceMinor, invitationCount, bookingCount, financialRecordCount, auditEventCount, mediaCount]
      properties:
        groupId: { type: string }
        groupName: { type: string }
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        version: { type: integer, format: int64, minimum: 1 }
        memberCount: { type: integer, format: int64, minimum: 0 }
        openBalanceMinor:
          $ref: '#/components/schemas/MinorUnits'
          description: Signed consolidated balance of all member receivable accounts in the group's smallest currency unit.
        invitationCount: { type: integer, format: int64, minimum: 0 }
        bookingCount: { type: integer, format: int64, minimum: 0 }
        financialRecordCount:
          type: integer
          format: int64
          minimum: 0
          description: Ledger-backed financial records selected by the purge impact query.
        auditEventCount: { type: integer, format: int64, minimum: 0 }
        mediaCount: { type: integer, format: int64, minimum: 0 }
    GroupPurgeCommand:
      type: object
      additionalProperties: false
      required: [groupName]
      properties:
        groupName: { type: string, minLength: 1, maxLength: 120 }
    SystemAuditEvent:
      type: object
      additionalProperties: false
      required: [id, action, resourceType, metadata, occurredAt]
      properties:
        id: { type: string }
        actorUserId: { type: string }
        actorDisplayName:
          type: string
          description: Current display name of the actor account; absent for system-generated events.
        action: { type: string }
        resourceType: { type: string }
        resourceId: { type: string }
        metadata:
          type: object
          additionalProperties: true
          description: Secret-free bounded metadata; purge events retain only group identity, actor, time, and deletion counts.
        occurredAt: { type: string, format: date-time }
    AuditFilterOptions:
      type: object
      additionalProperties: false
      required: [actions, resourceTypes, actionResourceTypes]
      properties:
        actions:
          type: array
          description: Distinct persisted action values in stable case-insensitive order.
          items: { type: string }
        resourceTypes:
          type: array
          description: Distinct persisted resource-type values in stable case-insensitive order.
          items: { type: string }
        actionResourceTypes:
          type: object
          description: Persisted resource types observed for each action; clients use this relationship to constrain dependent action filters.
          additionalProperties:
            type: array
            items: { type: string }
        actors:
          type: array
          description: Distinct group-membership actors present in group-scoped audit events; omitted for system audit.
          items: { $ref: '#/components/schemas/MemberFilterOption' }
    MemberFilterOption:
      type: object
      additionalProperties: false
      required: [membershipId, displayName]
      properties:
        membershipId: { type: string }
        displayName: { type: string }
        avatarUrl: { type: string }
    BookingFilterOptions:
      type: object
      additionalProperties: false
      required: [members]
      properties:
        members:
          type: array
          items: { $ref: '#/components/schemas/MemberFilterOption' }
    ActivityFilterOptions:
      type: object
      additionalProperties: false
      required: [kinds, members, categories, products]
      properties:
        kinds:
          type: array
          description: Distinct transaction kinds present in the caller's authorized unified feed.
          items: { $ref: '#/components/schemas/ActivityKind' }
        members:
          type: array
          description: Distinct targets present in the caller's authorized unified feed.
          items: { $ref: '#/components/schemas/MemberFilterOption' }
        categories:
          type: array
          description: Distinct booking categories present in the caller's authorized unified feed.
          items: { $ref: '#/components/schemas/ActivityCategoryFilterOption' }
        products:
          type: array
          description: Distinct booking products present in the caller's authorized unified feed, including retained historical products.
          items: { $ref: '#/components/schemas/ActivityProductFilterOption' }
    ActivityCategoryFilterOption:
      type: object
      additionalProperties: false
      required: [categoryId, name, icon]
      properties:
        categoryId: { type: string }
        name: { type: string }
        icon: { $ref: '#/components/schemas/CategoryIcon' }
    ActivityProductFilterOption:
      type: object
      additionalProperties: false
      required: [productId, categoryId, name]
      properties:
        productId: { type: string }
        categoryId: { type: string }
        name: { type: string }
        imageUrl:
          type: string
          description: Current same-origin image projection when the visible product still has an image.
    PasswordResetRequest:
      type: object
      additionalProperties: false
      required: [email]
      properties:
        email: { type: string, format: email, maxLength: 254 }
    PasswordResetConfirmation:
      type: object
      additionalProperties: false
      required: [token, newPassword]
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 256
          description: One-time token read by the client from the reset URL fragment.
        newPassword: { type: string, format: password, minLength: 12, maxLength: 1024 }
    AccountActionTokenCommand:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 256
          description: One-time token read by the client from the confirmation URL fragment.
    ProfileUpdate:
      type: object
      additionalProperties: false
      required: [displayName]
      properties:
        displayName: { type: string, minLength: 1, maxLength: 120 }
    GroupPreferenceUpdate:
      type: object
      additionalProperties: false
      required: [defaultGroupId]
      properties:
        defaultGroupId:
          type: [string, 'null']
          description: Active fixed default group, or null to reopen the most recently used active group.
    GroupPreference:
      type: object
      additionalProperties: false
      required: [defaultGroupId]
      properties:
        defaultGroupId:
          type: [string, 'null']
          readOnly: true
    LastUsedGroupUpdate:
      type: object
      additionalProperties: false
      required: [groupId]
      properties:
        groupId: { type: string, minLength: 1 }
    ColorMode:
      type: string
      enum: [SYSTEM, LIGHT, DARK]
    ThemeId:
      type: string
      enum: [TEAMTALER, NRW, TIEF_IM_WESTEN, FIRE]
    Appearance:
      type: object
      additionalProperties: false
      required: [colorMode]
      properties:
        colorMode: { $ref: '#/components/schemas/ColorMode' }
    ThemePreference:
      type: object
      additionalProperties: false
      required: [themeOverride]
      properties:
        themeOverride:
          oneOf:
            - $ref: '#/components/schemas/ThemeId'
            - type: 'null'
          description: Null follows the group default; a theme identifier remains a per-group override.
    PasswordUpdate:
      type: object
      additionalProperties: false
      required: [currentPassword, newPassword]
      properties:
        currentPassword: { type: string, format: password, minLength: 1, maxLength: 1024 }
        newPassword: { type: string, format: password, minLength: 12, maxLength: 1024 }
    EmailChangeRequest:
      type: object
      additionalProperties: false
      required: [newEmail, currentPassword]
      properties:
        newEmail: { type: string, format: email, maxLength: 254 }
        currentPassword: { type: string, format: password, minLength: 1, maxLength: 1024 }
    InvitationAcceptance:
      type: object
      required: [token, password, expectedAccountState]
      additionalProperties: false
      properties:
        token: { type: string, minLength: 1 }
        displayName:
          type: string
          maxLength: 120
          description: Required for a new account and ignored for an existing account.
        password: { type: string, format: password, minLength: 12, maxLength: 1024 }
        expectedAccountState:
          $ref: '#/components/schemas/InvitationAccountState'
    InvitationPreviewCommand:
      type: object
      required: [token]
      properties:
        token: { type: string, minLength: 1 }
    InvitationPreview:
      type: object
      required: [displayName, accountState]
      additionalProperties: false
      properties:
        displayName: { type: string, maxLength: 120 }
        accountState:
          $ref: '#/components/schemas/InvitationAccountState'
    InvitationAccountState:
      type: string
      enum: [NEW, EXISTING]
      description: Credential state observed for the invitation's stable global identity.
    PublicJoinTokenCommand:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token:
          type: string
          minLength: 1
          description: Opaque URL-fragment token that is hashed before lookup.
    PublicJoinRegistration:
      type: object
      additionalProperties: false
      required: [joinToken, email, displayName, password]
      properties:
        joinToken: { type: string, minLength: 1 }
        email: { type: string, format: email, maxLength: 254 }
        displayName: { type: string, minLength: 1, maxLength: 120 }
        password: { type: string, format: password, minLength: 12, maxLength: 1024 }
    PublicJoinResendCommand:
      type: object
      additionalProperties: false
      required: [joinToken, email]
      properties:
        joinToken: { type: string, minLength: 1 }
        email: { type: string, format: email, maxLength: 254 }
    PublicJoinVerificationCommand:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token:
          type: string
          minLength: 1
          description: One-time mailbox-verification token from the URL fragment.
    PublicJoinPreview:
      type: object
      additionalProperties: false
      required: [groupName, expiresAt]
      properties:
        groupName: { type: string, minLength: 1, maxLength: 120 }
        expiresAt:
          type: [string, 'null']
          format: date-time
          description: Null for an unlimited public link.
    VerificationRequired:
      type: object
      additionalProperties: false
      required: [verificationRequired]
      properties:
        verificationRequired: { const: true }
    PublicJoinLink:
      type: object
      additionalProperties: false
      required: [enabled, expired, expiresAt, version, emailVerificationAvailable]
      properties:
        enabled:
          type: boolean
          description: Persisted administrator-controlled state.
        expired:
          type: boolean
          readOnly: true
          description: True when an enabled link has reached its finite expiry.
        expiresAt:
          type: [string, 'null']
          format: date-time
          description: Null for unlimited availability or a disabled link.
        acceptUrl:
          type: string
          format: uri
          readOnly: true
          description: Present only for an enabled and unexpired link. The secret remains in the URL fragment.
        version:
          type: integer
          format: int64
          minimum: 0
          description: Zero means no public join-link resource has been persisted yet.
        createdAt: { type: string, format: date-time, readOnly: true }
        updatedAt: { type: string, format: date-time, readOnly: true }
        emailVerificationAvailable:
          type: boolean
          readOnly: true
          description: Whether SMTP delivery and authenticated token encryption are configured.
    PublicJoinLinkUpdate:
      type: object
      additionalProperties: false
      required: [enabled, expiresAt]
      properties:
        enabled: { type: boolean }
        expiresAt:
          type: [string, 'null']
          format: date-time
          description: Null means unlimited while enabled; finite values must be between one hour and 365 days in the future.
    Session:
      type: object
      required: [user, csrfToken, colorMode, groups, activeGroupId, defaultGroupId, systemRoles]
      properties:
        user: { $ref: '#/components/schemas/User' }
        csrfToken: { type: string }
        colorMode: { $ref: '#/components/schemas/ColorMode' }
        groups:
          type: array
          items: { $ref: '#/components/schemas/Group' }
        activeGroupId:
          type: [string, 'null']
          description: Current active group, or null for an authenticated account with no available group.
        defaultGroupId:
          type: [string, 'null']
          description: Fixed active group selected for login, or null for most-recently-used behavior.
        systemRoles:
          type: array
          uniqueItems: true
          maxItems: 1
          items: { $ref: '#/components/schemas/SystemRole' }
          description: Client-navigation hint only; protected endpoints reload current assignments from SQLite.
    User:
      type: object
      required: [id, email, displayName]
      properties:
        id: { type: string }
        email: { type: string, format: email }
        displayName: { type: string }
        avatarUrl: { type: string }
    Group:
      type: object
      required: [id, name, currency, defaultTheme, membership]
      properties:
        id: { type: string }
        name: { type: string }
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        logoUrl: { type: string }
        defaultTheme: { $ref: '#/components/schemas/ThemeId' }
        membership: { $ref: '#/components/schemas/Membership' }
    GroupSettings:
      type: object
      additionalProperties: false
      required: [defaultTheme, notificationEmailsEnabled, notificationEmailDeliveryAvailable, settlementsEnabled, defaultRoleId, ownBookingReasonMode, foreignBookingReasonMode, ownPaymentReasonMode, otherPaymentReasonMode, foreignBookingReasonRequired, ownPaymentReasonRequired, otherPaymentReasonRequired, paymentMethods, bookingReasons, paymentReasons]
      properties:
        defaultTheme: { $ref: '#/components/schemas/ThemeId' }
        notificationEmailsEnabled:
          type: boolean
          deprecated: true
          description: Legacy migration source retained for compatibility; new delivery policy uses `/notification-settings` and `/notification-preferences`.
        notificationEmailDeliveryAvailable:
          type: boolean
          readOnly: true
          description: Whether the running server has complete SMTP delivery configuration.
        settlementsEnabled:
          type: boolean
          description: Whether administrators may close the unchanged technical open period and create settlement statements.
        defaultRoleId:
          type: [string, 'null']
          description: Group-owned non-administrative role preselected for new invitations and used by CSV rows without roles.
        ownBookingReasonMode:
          $ref: '#/components/schemas/ReasonMode'
        foreignBookingReasonMode:
          $ref: '#/components/schemas/ReasonMode'
        ownPaymentReasonMode:
          $ref: '#/components/schemas/ReasonMode'
        otherPaymentReasonMode:
          $ref: '#/components/schemas/ReasonMode'
        foreignBookingReasonRequired:
          type: boolean
          deprecated: true
          description: Compatibility alias for foreignBookingReasonMode=REQUIRED.
        ownPaymentReasonRequired:
          type: boolean
          deprecated: true
          description: Compatibility alias for ownPaymentReasonMode=REQUIRED.
        otherPaymentReasonRequired:
          type: boolean
          deprecated: true
          description: Compatibility alias for otherPaymentReasonMode=REQUIRED.
        paymentMethods:
          type: array
          minItems: 1
          maxItems: 20
          items: { $ref: '#/components/schemas/PaymentMethod' }
        bookingReasons:
          type: array
          maxItems: 50
          items: { $ref: '#/components/schemas/ConfigurableItem' }
        paymentReasons:
          type: array
          maxItems: 50
          items: { $ref: '#/components/schemas/ConfigurableItem' }
    GroupSettingsUpdate:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        defaultTheme: { $ref: '#/components/schemas/ThemeId' }
        notificationEmailsEnabled:
          type: boolean
          deprecated: true
          description: Legacy compatibility field; new clients must use the dedicated notification policy endpoints.
        settlementsEnabled:
          type: boolean
          description: Enables or disables period closing without changing periods, ledger entries, balances, or settlement history.
        defaultRoleId:
          type: string
          minLength: 1
          description: Group-owned role that must not grant GROUP_ADMINISTRATION or MEMBER_MANAGEMENT. It cannot be cleared after being set and may be changed only with MEMBER_MANAGEMENT.
        ownBookingReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        foreignBookingReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        ownPaymentReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        otherPaymentReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        foreignBookingReasonRequired:
          type: boolean
          deprecated: true
        ownPaymentReasonRequired:
          type: boolean
          deprecated: true
        otherPaymentReasonRequired:
          type: boolean
          deprecated: true
        paymentMethods:
          type: array
          minItems: 1
          maxItems: 20
          items: { $ref: '#/components/schemas/PaymentMethodUpdate' }
        bookingReasons:
          type: array
          maxItems: 50
          items: { $ref: '#/components/schemas/ConfigurableItem' }
        paymentReasons:
          type: array
          maxItems: 50
          items: { $ref: '#/components/schemas/ConfigurableItem' }
    NotificationChannel:
      type: string
      enum: [EMAIL, PUSH]
    NotificationEventDefinition:
      type: object
      required: [type, category, label, description, route, pushTtlSeconds, pushUrgency, defaultEnabled, reminder, supportedChannels]
      properties:
        type:
          type: string
          enum: [BOOKING_ASSIGNED, BOOKING_REVERSED, PAYMENT_RECORDED, PAYMENT_REVERSED, SETTLEMENT_CREATED, SETTLEMENT_DUE_SOON, SETTLEMENT_OVERDUE]
        category: { type: string }
        label: { type: string }
        description: { type: string }
        route:
          type: string
          pattern: '^/'
          description: Same-origin application route used when a delivered notification is opened.
        pushTtlSeconds:
          type: integer
          minimum: 0
          description: Maximum lifetime of the corresponding Web Push message at the push service.
        pushUrgency:
          type: string
          enum: [very-low, low, normal, high]
        defaultEnabled: { type: boolean }
        reminder: { type: boolean }
        supportedChannels: { type: array, items: { $ref: '#/components/schemas/NotificationChannel' } }
    GroupNotificationEventSetting:
      allOf:
        - $ref: '#/components/schemas/NotificationEventDefinition'
        - type: object
          required: [enabled]
          properties: { enabled: { type: boolean } }
    GroupNotificationSettings:
      type: object
      additionalProperties: false
      required: [version, timezone, dueSoonLeadDays, overdueRepeatDays, availableChannels, events, updatedAt]
      properties:
        version: { type: integer, format: int64, minimum: 1 }
        timezone: { type: string, default: Europe/Berlin }
        dueSoonLeadDays: { type: integer, minimum: 1, maximum: 30, default: 3 }
        overdueRepeatDays: { type: integer, minimum: 0, maximum: 90, default: 7 }
        availableChannels: { type: array, items: { $ref: '#/components/schemas/NotificationChannel' } }
        events: { type: array, minItems: 7, maxItems: 7, items: { $ref: '#/components/schemas/GroupNotificationEventSetting' } }
        updatedAt: { type: string, format: date-time }
    GroupNotificationSettingsUpdate:
      type: object
      additionalProperties: false
      required: [timezone, dueSoonLeadDays, overdueRepeatDays, events]
      properties:
        timezone: { type: string }
        dueSoonLeadDays: { type: integer, minimum: 1, maximum: 30 }
        overdueRepeatDays: { type: integer, minimum: 0, maximum: 90 }
        events:
          type: array
          minItems: 7
          maxItems: 7
          items:
            type: object
            additionalProperties: false
            required: [type, enabled]
            properties:
              type: { $ref: '#/components/schemas/NotificationEventDefinition/properties/type' }
              enabled: { type: boolean }
    NotificationEventPreference:
      allOf:
        - $ref: '#/components/schemas/NotificationEventDefinition'
        - type: object
          required: [enabled, email, push, emailAvailable, pushAvailable]
          properties:
            enabled: { type: boolean }
            email: { type: boolean }
            push: { type: boolean }
            emailAvailable: { type: boolean }
            pushAvailable: { type: boolean }
    NotificationPreferences:
      type: object
      additionalProperties: false
      required: [version, availableChannels, events]
      properties:
        version: { type: integer, format: int64, minimum: 1 }
        availableChannels: { type: array, items: { $ref: '#/components/schemas/NotificationChannel' } }
        events: { type: array, minItems: 7, maxItems: 7, items: { $ref: '#/components/schemas/NotificationEventPreference' } }
    NotificationPreferencesUpdate:
      type: object
      additionalProperties: false
      required: [events]
      properties:
        events:
          type: array
          minItems: 1
          maxItems: 7
          items:
            type: object
            additionalProperties: false
            required: [type]
            properties:
              type: { $ref: '#/components/schemas/NotificationEventDefinition/properties/type' }
              email: { type: boolean }
              push: { type: boolean }
    ConfigurableItem:
      type: object
      additionalProperties: false
      required: [id, label]
      properties:
        id: { type: string, minLength: 1, maxLength: 120 }
        label: { type: string, minLength: 1, maxLength: 120 }
    AttachmentMode:
      type: string
      enum: [OFF, OPTIONAL, REQUIRED]
      description: Controls whether a receipt is rejected, accepted, or mandatory for the payment method.
    PayPalMePaymentTarget:
      type: object
      additionalProperties: false
      required: [type, paypalMeHandle]
      properties:
        type:
          type: string
          const: PAYPAL_ME
        paypalMeHandle:
          type: string
          minLength: 1
          maxLength: 20
          pattern: '^[A-Za-z0-9]+$'
          description: Normalized PayPal.Me handle.
    SepaTransferPaymentTarget:
      type: object
      additionalProperties: false
      required: [type, recipientName, iban]
      properties:
        type:
          type: string
          const: SEPA_TRANSFER
        recipientName:
          type: string
          minLength: 1
          maxLength: 70
        iban:
          type: string
          minLength: 15
          maxLength: 34
          description: Whitespace-free uppercase IBAN with a valid MOD-97 checksum.
        bic:
          type: string
          pattern: '^[A-Z]{6}[A-Z0-9]{2}([A-Z0-9]{3})?$'
          description: Normalized 8- or 11-character BIC. Optional only when the IBAN country belongs to the EEA; required otherwise.
    PaymentTarget:
      oneOf:
        - $ref: '#/components/schemas/PayPalMePaymentTarget'
        - $ref: '#/components/schemas/SepaTransferPaymentTarget'
      discriminator:
        propertyName: type
        mapping:
          PAYPAL_ME: '#/components/schemas/PayPalMePaymentTarget'
          SEPA_TRANSFER: '#/components/schemas/SepaTransferPaymentTarget'
    PayPalMePaymentTargetUpdate:
      type: object
      additionalProperties: false
      required: [type, paypalMeHandle]
      properties:
        type:
          type: string
          const: PAYPAL_ME
        paypalMeHandle:
          type: string
          minLength: 1
          maxLength: 120
          description: A 1-20 character ASCII-alphanumeric handle or a canonical paypal.me HTTPS URL containing only that handle.
    SepaTransferPaymentTargetUpdate:
      type: object
      additionalProperties: false
      required: [type, recipientName, iban]
      properties:
        type:
          type: string
          const: SEPA_TRANSFER
        recipientName:
          type: string
          minLength: 1
          maxLength: 70
        iban:
          type: string
          minLength: 15
          maxLength: 42
          description: IBAN input; non-control Unicode whitespace is removed and letters are uppercased before MOD-97 validation. Control characters are rejected.
        bic:
          type: string
          maxLength: 14
          description: BIC input, required for IBAN countries outside the EEA. Surrounding whitespace is removed and letters are uppercased before validation.
    PaymentTargetUpdate:
      oneOf:
        - $ref: '#/components/schemas/PayPalMePaymentTargetUpdate'
        - $ref: '#/components/schemas/SepaTransferPaymentTargetUpdate'
      discriminator:
        propertyName: type
        mapping:
          PAYPAL_ME: '#/components/schemas/PayPalMePaymentTargetUpdate'
          SEPA_TRANSFER: '#/components/schemas/SepaTransferPaymentTargetUpdate'
    PaymentMethod:
      type: object
      additionalProperties: false
      required: [id, label, attachmentMode, paymentTarget]
      properties:
        id: { type: string, minLength: 1, maxLength: 120 }
        label: { type: string, minLength: 1, maxLength: 120 }
        attachmentMode: { $ref: '#/components/schemas/AttachmentMode' }
        paymentTarget:
          oneOf:
            - $ref: '#/components/schemas/PaymentTarget'
            - type: 'null'
          description: Member-visible external payment instruction, or null when this method has none.
    PaymentMethodUpdate:
      type: object
      additionalProperties: false
      required: [id, label, attachmentMode]
      properties:
        id: { type: string, minLength: 1, maxLength: 120 }
        label: { type: string, minLength: 1, maxLength: 120 }
        attachmentMode: { $ref: '#/components/schemas/AttachmentMode' }
        paymentTarget:
          oneOf:
            - $ref: '#/components/schemas/PaymentTargetUpdate'
            - type: 'null'
          description: Omission preserves the target of the same stable method id, null clears it, and an object sets or replaces it. A new id with omission starts without a target.
    ReasonMode:
      type: string
      enum: [OFF, OPTIONAL, REQUIRED]
      description: Controls whether the corresponding transaction reason is hidden, optional, or mandatory.
    TransactionSettings:
      type: object
      additionalProperties: false
      required: [settlementsEnabled, ownBookingReasonMode, foreignBookingReasonMode, ownPaymentReasonMode, otherPaymentReasonMode, foreignBookingReasonRequired, ownPaymentReasonRequired, otherPaymentReasonRequired, paymentMethods, bookingReasons, paymentReasons]
      properties:
        settlementsEnabled:
          type: boolean
          description: Whether settlement-period references and actions should be shown to active members.
        ownBookingReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        foreignBookingReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        ownPaymentReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        otherPaymentReasonMode: { $ref: '#/components/schemas/ReasonMode' }
        foreignBookingReasonRequired: { type: boolean, deprecated: true }
        ownPaymentReasonRequired: { type: boolean, deprecated: true }
        otherPaymentReasonRequired: { type: boolean, deprecated: true }
        paymentMethods:
          type: array
          minItems: 1
          maxItems: 20
          items: { $ref: '#/components/schemas/PaymentMethod' }
        bookingReasons:
          type: array
          maxItems: 50
          items: { $ref: '#/components/schemas/ConfigurableItem' }
        paymentReasons:
          type: array
          maxItems: 50
          items: { $ref: '#/components/schemas/ConfigurableItem' }
    Membership:
      type: object
      required: [id, groupId, userId, displayName, isTemporaryGuest, status, themeOverride, roleIds, effectiveGrants, roleAssignmentsVersion, roles, groupPermissions, categoryGrants]
      properties:
        id: { type: string }
        groupId: { type: string }
        userId:
          type: string
          description: Required identity reference. A temporary guest points to a credentialless user; claiming with an existing account may rebind this field while the membership id and accounting history remain stable.
        email:
          type: [string, 'null']
          format: email
          description: Omitted or null only for a credentialless temporary guest; synthetic addresses are never generated.
        displayName: { type: string }
        isTemporaryGuest:
          type: boolean
          readOnly: true
          description: Server-derived only from missing email and password credentials.
        avatarUrl: { type: string }
        themeOverride:
          oneOf:
            - $ref: '#/components/schemas/ThemeId'
            - type: 'null'
          description: Null follows the group's default theme.
        status:
          type: string
          enum: [ACTIVE, ARCHIVED, DELETED]
          description: DELETED is a retained historical tombstone that is excluded from the member directory.
        roleIds:
          type: array
          uniqueItems: true
          description: Stable identifiers of every assigned group-owned role.
          items: { type: string }
        effectiveGrants:
          type: array
          uniqueItems: true
          readOnly: true
          description: Union of assigned role grants after permission implications are expanded.
          items: { $ref: '#/components/schemas/PermissionGrant' }
        roleAssignmentsVersion:
          type: integer
          format: int64
          minimum: 1
          readOnly: true
          description: Whole-assignment-set version used to construct the membership roles ETag.
        roles:
          type: array
          uniqueItems: true
          deprecated: true
          description: Deprecated legacy preset-role strings derived from current preset assignments.
          items: { $ref: '#/components/schemas/LegacyRole' }
        groupPermissions:
          type: array
          uniqueItems: true
          deprecated: true
          description: Deprecated compatibility projection for migrated self-payment access.
          items: { $ref: '#/components/schemas/GroupPermission' }
        categoryGrants:
          deprecated: true
          description: Deprecated compatibility field; always empty after migration `0017`.
          $ref: '#/components/schemas/LegacyCategoryGrants'
    PermissionKey:
      type: string
      enum:
        - GROUP_ADMINISTRATION
        - MEMBER_MANAGEMENT
        - ROLE_MANAGEMENT
        - FINANCE_MANAGEMENT
        - CATALOG_MANAGEMENT
        - VIEW_MEMBER_DIRECTORY
        - VIEW_GROUP_STATISTICS
        - VIEW_ALL_BOOKING_ACTIVITY
        - RECORD_OWN_PAYMENT
        - CREATE_OWN_BOOKING
        - VOID_OWN_BOOKING
        - VOID_ANY_BOOKING
        - BOOK_FOR_OTHERS
        - BOOK_FOR_GUESTS
    PermissionScopeType:
      type: string
      enum: [GROUP, CATEGORY, PRODUCT]
    PermissionScope:
      description: Tenant-bound permission resource scope. Only the GROUP variant is accepted by v1 mutations.
      oneOf:
        - type: object
          additionalProperties: false
          required: [type]
          properties:
            type: { const: GROUP }
        - type: object
          additionalProperties: false
          required: [type, categoryId]
          properties:
            type: { const: CATEGORY }
            categoryId: { type: string }
        - type: object
          additionalProperties: false
          required: [type, productId]
          properties:
            type: { const: PRODUCT }
            productId: { type: string }
    PermissionDefinition:
      type: object
      additionalProperties: false
      required: [key, description, allowedScopes, implies]
      properties:
        key: { $ref: '#/components/schemas/PermissionKey' }
        description:
          type: string
          maxLength: 500
          description: Stable English technical explanation; localized UI labels and help text remain client-owned.
        allowedScopes:
          type: array
          uniqueItems: true
          minItems: 1
          items: { $ref: '#/components/schemas/PermissionScopeType' }
        implies:
          type: array
          uniqueItems: true
          description: Permission keys transitively included by this permission.
          items: { $ref: '#/components/schemas/PermissionKey' }
    PermissionGrant:
      type: object
      additionalProperties: false
      required: [permission, scope]
      properties:
        permission: { $ref: '#/components/schemas/PermissionKey' }
        scope: { $ref: '#/components/schemas/PermissionScope' }
    RolePresetKey:
      type: string
      description: Reserved system-role identity. Ordinary seeded roles have no preset key.
      enum: [GROUP_ADMINISTRATOR]
    Role:
      type: object
      additionalProperties: false
      required: [id, groupId, name, description, nameLocked, deletable, grants, version, memberCount, pendingInvitationCount, createdAt, updatedAt]
      properties:
        id: { type: string }
        groupId: { type: string }
        presetKey: { $ref: '#/components/schemas/RolePresetKey' }
        name:
          type: string
          minLength: 1
          maxLength: 120
          description: Trimmed, control-character-free, and case-insensitively unique within the group.
        description: { type: string, maxLength: 500 }
        nameLocked:
          type: boolean
          description: True only when the role name is protected from changes.
        deletable:
          type: boolean
          description: False for protected presets; an assigned editable role still returns a deletion conflict.
        grants:
          type: array
          uniqueItems: true
          items: { $ref: '#/components/schemas/PermissionGrant' }
        version: { type: integer, format: int64, minimum: 1 }
        memberCount: { type: integer, format: int64, minimum: 0 }
        pendingInvitationCount: { type: integer, format: int64, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    RoleCommand:
      type: object
      additionalProperties: false
      required: [name, grants]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
          description: Trimmed, control-character-free, and case-insensitively unique within the group.
        description:
          type: string
          maxLength: 500
          description: Trimmed and control-character-free role explanation.
        grants:
          type: array
          uniqueItems: true
          items: { $ref: '#/components/schemas/PermissionGrant' }
    RoleAssignmentSubjectType:
      type: string
      enum: [MEMBERSHIP, INVITATION]
    RoleAssignment:
      type: object
      additionalProperties: false
      required: [subjectType, subjectId, roleIds, version]
      properties:
        subjectType: { $ref: '#/components/schemas/RoleAssignmentSubjectType' }
        subjectId: { type: string }
        roleIds:
          type: array
          minItems: 1
          uniqueItems: true
          items: { type: string }
        version: { type: integer, format: int64, minimum: 1 }
    RoleAssignmentUpdate:
      type: object
      additionalProperties: false
      required: [roleIds]
      properties:
        roleIds:
          type: array
          minItems: 1
          uniqueItems: true
          items: { type: string }
    LegacyRole:
      type: string
      deprecated: true
      enum: [ADMIN, FINANCE_MANAGER, CATALOG_MANAGER]
    GroupPermission:
      type: string
      deprecated: true
      enum: [SELF_RECORD_PAYMENT]
    CategoryPermission:
      type: string
      deprecated: true
      enum: [ASSIGN_TO_OTHERS, VOID_BOOKINGS]
    LegacyCategoryGrants:
      type: object
      deprecated: true
      additionalProperties:
        type: array
        uniqueItems: true
        items: { $ref: '#/components/schemas/CategoryPermission' }
    LegacyCategoryGrantWrite:
      type: object
      deprecated: true
      maxProperties: 0
      additionalProperties: false
      description: Must be empty; non-empty legacy category grants return `422`.
    PermissionUpdate:
      type: object
      required: [roles, categoryGrants]
      properties:
        roles:
          type: array
          uniqueItems: true
          deprecated: true
          description: Replaces only legacy preset assignments and preserves every custom role assignment.
          items: { $ref: '#/components/schemas/LegacyRole' }
        groupPermissions:
          type: array
          uniqueItems: true
          deprecated: true
          items: { $ref: '#/components/schemas/GroupPermission' }
        categoryGrants:
          $ref: '#/components/schemas/LegacyCategoryGrantWrite'
    TemporaryGuestRenameCommand:
      type: object
      additionalProperties: false
      required: [displayName]
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 120
          description: Trimmed, control-character-free replacement display name.
    MemberReactivationCommand:
      type: object
      additionalProperties: false
      required: [roleIds]
      properties:
        displayName:
          type: string
          minLength: 1
          maxLength: 120
          description: Optional replacement name for an archived temporary guest; ignored for credentialed memberships.
        roleIds:
          type: array
          uniqueItems: true
          description: Complete role set for a credentialed membership. Temporary guests must provide an empty array.
          items: { type: string, minLength: 1 }
    TemporaryGuestClaimInvitationCommand:
      type: object
      additionalProperties: false
      required: [email, roleIds]
      properties:
        email: { type: string, format: email, maxLength: 254 }
        roleIds:
          type: array
          minItems: 1
          uniqueItems: true
          items: { type: string, minLength: 1 }
    TemporaryGuestClaimInvitationResult:
      type: object
      additionalProperties: false
      required: [invitation, acceptUrl]
      properties:
        invitation: { $ref: '#/components/schemas/Invitation' }
        acceptUrl:
          type: string
          description: Secret-bearing fallback URL returned only when the conflict-protected claim invitation is created.
    InvitationCommand:
      type: object
      description: "`roleIds` cannot be combined with deprecated `roles`, `groupPermissions`, or non-empty `categoryGrants`."
      required: [email]
      properties:
        email: { type: string, format: email, maxLength: 254 }
        displayName:
          type: string
          maxLength: 120
          description: Optional suggested name for a new account.
        roleIds:
          type: array
          minItems: 1
          uniqueItems: true
          description: Complete explicit group-owned role selection.
          items: { type: string }
        roles:
          type: array
          uniqueItems: true
          deprecated: true
          description: Deprecated preset-role input; custom invitation roles remain intact on legacy updates.
          items: { $ref: '#/components/schemas/LegacyRole' }
        groupPermissions:
          type: array
          uniqueItems: true
          deprecated: true
          items: { $ref: '#/components/schemas/GroupPermission' }
        categoryGrants:
          $ref: '#/components/schemas/LegacyCategoryGrantWrite'
    InvitationUpdate:
      type: object
      description: "`roleIds` cannot be combined with deprecated `roles`, `groupPermissions`, or non-empty `categoryGrants`."
      properties:
        displayName: { type: string, maxLength: 120 }
        roleIds:
          type: array
          minItems: 1
          uniqueItems: true
          description: Complete explicit selected role set.
          items: { type: string }
        roles:
          type: array
          uniqueItems: true
          deprecated: true
          description: Deprecated preset-role input; custom invitation roles remain intact on legacy updates.
          items: { $ref: '#/components/schemas/LegacyRole' }
        groupPermissions:
          type: array
          uniqueItems: true
          deprecated: true
          items: { $ref: '#/components/schemas/GroupPermission' }
        categoryGrants:
          $ref: '#/components/schemas/LegacyCategoryGrantWrite'
    Invitation:
      type: object
      required: [id, groupId, email, roleIds, roleAssignmentsVersion, roles, groupPermissions, categoryGrants, expiresAt, emailDeliveryStatus]
      properties:
        id: { type: string }
        groupId: { type: string }
        targetMembershipId:
          type: string
          readOnly: true
          description: Present only for a temporary-guest claim invitation; acceptance preserves this membership identity and its history.
        email: { type: string, format: email }
        displayName: { type: string }
        roleIds:
          type: array
          minItems: 1
          uniqueItems: true
          items: { type: string }
        roleAssignmentsVersion:
          type: integer
          format: int64
          minimum: 1
          readOnly: true
          description: Whole-assignment-set version used to construct the pending-invitation roles ETag.
        roles:
          type: array
          deprecated: true
          items: { $ref: '#/components/schemas/LegacyRole' }
        groupPermissions:
          type: array
          uniqueItems: true
          deprecated: true
          items: { $ref: '#/components/schemas/GroupPermission' }
        categoryGrants:
          deprecated: true
          $ref: '#/components/schemas/LegacyCategoryGrants'
        expiresAt: { type: string, format: date-time }
        acceptedAt: { type: string, format: date-time }
        revokedAt: { type: string, format: date-time }
        emailDeliveryStatus: { $ref: '#/components/schemas/EmailDeliveryStatus' }
        emailSentAt: { type: string, format: date-time }
        emailFailureCode: { type: string }
    EmailDeliveryStatus:
      type: string
      enum: [NOT_REQUESTED, PENDING, SENDING, SENT, FAILED, CANCELLED]
    InvitationImportResult:
      type: object
      required: [summary, rows]
      properties:
        summary: { $ref: '#/components/schemas/InvitationImportSummary' }
        rows:
          type: array
          maxItems: 100
          items: { $ref: '#/components/schemas/InvitationImportRow' }
    InvitationImportSummary:
      type: object
      required: [totalRows, created, invalid, skipped]
      properties:
        totalRows: { type: integer, minimum: 1, maximum: 100 }
        created: { type: integer, minimum: 0, maximum: 100 }
        invalid: { type: integer, minimum: 0, maximum: 100 }
        skipped: { type: integer, minimum: 0, maximum: 100 }
    InvitationImportRow:
      type: object
      required: [row, invitationStatus]
      properties:
        row: { type: integer, minimum: 2 }
        email: { type: string, format: email }
        displayName: { type: string, maxLength: 120 }
        invitationId: { type: string }
        invitationStatus:
          type: string
          enum: [CREATED, INVALID, SKIPPED_ALREADY_MEMBER, SKIPPED_ALREADY_INVITED]
        emailDeliveryStatus: { $ref: '#/components/schemas/EmailDeliveryStatus' }
        code:
          type: string
          enum: [invalid_email, invalid_display_name, display_name_too_long, duplicate_email, already_member, already_invited]
    InvitationEmailRetryResult:
      type: object
      required: [invitationId, emailDeliveryStatus]
      properties:
        invitationId: { type: string }
        emailDeliveryStatus:
          type: string
          const: PENDING
    InvitationEmailResendResult:
      type: object
      required: [invitationId, emailDeliveryStatus, expiresAt, acceptUrl]
      properties:
        invitationId: { type: string }
        emailDeliveryStatus:
          $ref: '#/components/schemas/EmailDeliveryStatus'
        expiresAt: { type: string, format: date-time }
        acceptUrl:
          type: string
          description: New fallback URL on the first idempotent response; empty on a replay.
    Category:
      type: object
      required: [id, groupId, name, icon, active, sortOrder, version]
      properties:
        id: { type: string }
        groupId: { type: string }
        name: { type: string }
        icon: { $ref: '#/components/schemas/CategoryIcon' }
        active: { type: boolean }
        sortOrder: { type: integer }
        version: { type: integer, format: int64 }
        products:
          type: array
          items: { $ref: '#/components/schemas/Product' }
    CategoryCommand:
      type: object
      required: [name, icon, sortOrder]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        icon: { $ref: '#/components/schemas/CategoryIcon' }
        sortOrder:
          type: integer
          description: Accepted for backward compatibility; newly created categories are appended to the current order.
    CategoryIcon:
      type: string
      enum: [other, drink, food, penalty, sport, event, transport, money]
    CategoryUpdate:
      allOf:
        - $ref: '#/components/schemas/CategoryCommand'
        - type: object
          required: [active, version]
          properties:
            active: { type: boolean }
            version: { type: integer, format: int64, minimum: 1 }
    CatalogOrderCommand:
      type: object
      required: [categoryIds, productIdsByCategory]
      additionalProperties: false
      properties:
        categoryIds:
          type: array
          maxItems: 1000
          uniqueItems: true
          items: { type: string, minLength: 1 }
        productIdsByCategory:
          type: object
          maxProperties: 1000
          additionalProperties:
            type: array
            maxItems: 1000
            uniqueItems: true
            items: { type: string, minLength: 1 }
    Product:
      type: object
      required: [id, groupId, categoryId, name, pricingMode, currency, active, sortOrder, version]
      properties:
        id: { type: string }
        groupId: { type: string }
        categoryId: { type: string }
        name: { type: string }
        priceMinor: { $ref: '#/components/schemas/MinorUnits' }
        pricingMode:
          type: string
          enum: [FIXED, USER_DEFINED]
          description: FIXED exposes priceMinor; USER_DEFINED requires the booking actor to submit a unit price.
        currency: { type: string }
        imageUrl: { type: string }
        active: { type: boolean }
        sortOrder: { type: integer }
        version: { type: integer, format: int64 }
      oneOf:
        - required: [priceMinor]
          properties:
            pricingMode: { const: FIXED }
        - properties:
            pricingMode: { const: USER_DEFINED }
            priceMinor: false
    ProductCommand:
      type: object
      required: [name, sortOrder]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        priceMinor: { type: integer, format: int64, minimum: 1, maximum: 100000000000 }
        pricingMode:
          type: string
          enum: [FIXED, USER_DEFINED]
          default: FIXED
        sortOrder:
          type: integer
          description: Accepted for backward compatibility; newly created products are appended within their category.
      oneOf:
        - required: [priceMinor]
          properties:
            pricingMode: { type: string, enum: [FIXED] }
        - required: [pricingMode]
          properties:
            pricingMode: { const: USER_DEFINED }
            priceMinor: false
    ProductUpdate:
      allOf:
        - $ref: '#/components/schemas/ProductCommand'
        - type: object
          required: [active, version]
          properties:
            active: { type: boolean }
            version: { type: integer, format: int64, minimum: 1 }
    BookingTarget:
      type: object
      additionalProperties: false
      required: [membershipId, displayName, isTemporaryGuest]
      properties:
        membershipId: { type: string }
        displayName: { type: string }
        avatarUrl: { type: string }
        isTemporaryGuest:
          type: boolean
          description: True only when the target has no login credentials.
    BookingContext:
      type: object
      additionalProperties: false
      required: [openPeriod, ownBalanceMinor, currentMembership, targets, canBookForGuests, ownBookingReasonMode, foreignBookingReasonMode, foreignBookingReasonRequired, bookingReasons]
      properties:
        openPeriod: { $ref: '#/components/schemas/Period' }
        ownBalanceMinor:
          $ref: '#/components/schemas/MinorUnits'
          description: Exact current balance for the authenticated membership only; no group total is included.
        currentMembership: { $ref: '#/components/schemas/Membership' }
        targets:
          type: array
          description: Booking-safe active targets; contains no email, role, grant, balance, or group-structure fields.
          items: { $ref: '#/components/schemas/BookingTarget' }
        canBookForGuests:
          type: boolean
          description: True only when BOOK_FOR_GUESTS is effective; controls inline temporary-guest creation.
        ownBookingReasonMode:
          $ref: '#/components/schemas/ReasonMode'
        foreignBookingReasonMode:
          $ref: '#/components/schemas/ReasonMode'
        foreignBookingReasonRequired:
          type: boolean
          deprecated: true
          description: Compatibility alias for foreignBookingReasonMode=REQUIRED.
        bookingReasons:
          type: array
          description: Ordered suggestions; clients must keep the reason field freely editable.
          items: { $ref: '#/components/schemas/ConfigurableItem' }
    BookingCommand:
      type: object
      required: [productId, productVersion, expectedPeriodId, quantity]
      properties:
        productId: { type: string }
        productVersion: { type: integer, format: int64, minimum: 1 }
        expectedPeriodId: { type: string }
        quantity: { type: integer, minimum: 1, maximum: 99 }
        unitPriceMinor:
          type: integer
          format: int64
          minimum: 1
          maximum: 100000000000
          description: Required for USER_DEFINED products and forbidden for FIXED products; interpreted as the unit price.
        targetMembershipId: { type: string }
        reason: { type: string, maxLength: 500 }
    BookingBatchCommand:
      type: object
      additionalProperties: false
      required: [productId, productVersion, expectedPeriodId, quantity]
      properties:
        productId: { type: string }
        productVersion: { type: integer, format: int64, minimum: 1 }
        expectedPeriodId: { type: string }
        quantity: { type: integer, minimum: 1, maximum: 99 }
        unitPriceMinor:
          type: integer
          format: int64
          minimum: 1
          maximum: 100000000000
          description: Required for USER_DEFINED products and forbidden for FIXED products; interpreted as the unit price applied to every target.
        targetMembershipIds:
          type: array
          minItems: 0
          maxItems: 100
          uniqueItems: true
          items: { type: string, minLength: 1 }
        temporaryGuestDisplayNames:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          description: |
            Trimmed, control-character-free names for new credentialless guests.
            Normalized duplicates within the command are rejected; names never
            identify or merge existing memberships. Requires BOOK_FOR_GUESTS.
          items: { type: string, minLength: 1, maxLength: 120 }
        reason:
          type: string
          maxLength: 500
          description: Shared reason stored on every created booking; required only when at least one target is another credentialed membership.
      anyOf:
        - required: [targetMembershipIds]
          properties:
            targetMembershipIds: { minItems: 1 }
        - required: [temporaryGuestDisplayNames]
      description: At least one target array must be present and non-empty; the two arrays may be combined but their total item count cannot exceed 100.
    BookingBulkLineItem:
      type: object
      additionalProperties: false
      required: [productId, productVersion, quantity]
      properties:
        productId: { type: string, minLength: 1 }
        productVersion: { type: integer, format: int64, minimum: 1 }
        quantity: { type: integer, minimum: 1, maximum: 99 }
        unitPriceMinor:
          type: integer
          format: int64
          minimum: 1
          maximum: 100000000000
          description: Required for USER_DEFINED products and forbidden for FIXED products; interpreted as this line's unit price for every target.
    BookingBulkCommand:
      type: object
      additionalProperties: false
      required: [expectedPeriodId, items]
      properties:
        expectedPeriodId: { type: string, minLength: 1 }
        items:
          type: array
          minItems: 1
          maxItems: 25
          description: Ordered, distinct product lines. Duplicate product IDs are rejected.
          items: { $ref: '#/components/schemas/BookingBulkLineItem' }
        targetMembershipIds:
          type: array
          minItems: 0
          maxItems: 100
          uniqueItems: true
          items: { type: string, minLength: 1 }
        temporaryGuestDisplayNames:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          description: |
            Trimmed, control-character-free names for new credentialless guests.
            Normalized duplicates within the command are rejected; names never
            identify or merge existing memberships. Requires BOOK_FOR_GUESTS.
          items: { type: string, minLength: 1, maxLength: 120 }
        reason:
          type: string
          maxLength: 500
          description: Shared reason stored on every created booking; required only when at least one target is another credentialed membership and the group requires one.
      anyOf:
        - required: [targetMembershipIds]
          properties:
            targetMembershipIds: { minItems: 1 }
        - required: [temporaryGuestDisplayNames]
      description: At least one target array must be non-empty. The target arrays may contain at most 100 entries combined, and items multiplied by targets may create at most 500 bookings.
    ActivityKind:
      type: string
      enum: [BOOKING, PAYMENT, REVERSAL, ADJUSTMENT]
    ActivityEntry:
      type: object
      additionalProperties: false
      required:
        - id
        - sourceId
        - kind
        - targetMembershipId
        - targetDisplayName
        - targetMembershipStatus
        - detailName
        - amountMinor
        - currency
        - occurredAt
        - status
        - canReverse
        - reversalReasonRequired
      properties:
        id:
          type: string
          description: Stable feed identity namespaced by transaction kind.
        sourceId:
          type: string
          description: Underlying booking, payment, or ledger identifier. Reversal rows intentionally expose no action.
        periodId: { type: string }
        kind: { $ref: '#/components/schemas/ActivityKind' }
        targetMembershipId: { type: string }
        targetDisplayName: { type: string }
        targetMembershipStatus: { type: string, enum: [ACTIVE, ARCHIVED, DELETED] }
        targetAvatarUrl: { type: string }
        actorMembershipId: { type: string }
        actorDisplayName:
          type: string
          description: Omitted for system adjustments without a persisted actor.
        actorMembershipStatus: { type: string, enum: [ACTIVE, ARCHIVED, DELETED] }
        actorAvatarUrl: { type: string }
        detailName:
          type: string
          description: Immutable product or payment-method label snapshot. Legacy payments created before label snapshots may return an empty value; paymentMethod remains available as the stable fallback identifier.
        detailNote: { type: string }
        paymentMethod:
          type: string
          description: Stable payment-method identifier used to localize built-in immutable label snapshots.
        categoryId: { type: string }
        categoryName: { type: string }
        productId: { type: string }
        quantity: { type: integer, minimum: 1 }
        amountMinor:
          allOf: [{ $ref: '#/components/schemas/MinorUnits' }]
          description: Signed visible receivable effect; reversal rows contain the exact opposite amount of their original.
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        occurredAt:
          type: string
          format: date-time
          description: Audited creation or reversal time; payment value dates remain available on the finance payment resource.
        status: { type: string, enum: [POSTED, REVERSED] }
        attachment: { $ref: '#/components/schemas/PaymentAttachmentSummary' }
        relatedActivityId:
          type: string
          description: Stable identity of the paired original or reversal. Present symmetrically on reversed originals and reversal rows.
        reversalSourceKind:
          type: string
          enum: [BOOKING, PAYMENT]
          description: Original transaction kind. Present on every REVERSAL row and omitted otherwise.
        canReverse:
          type: boolean
          description: Whether the current caller may invoke the source-specific reversal action.
        reversalReasonRequired:
          type: boolean
          description: Whether the currently available reversal requires a non-empty reason.
        reversalWithoutReasonUntil: { type: string, format: date-time }
      oneOf:
        - required: [relatedActivityId, reversalSourceKind]
          properties:
            kind: { type: string, enum: [REVERSAL] }
        - properties:
            kind: { type: string, enum: [BOOKING, PAYMENT, ADJUSTMENT] }
    Booking:
      type: object
      required: [id, groupId, periodId, categoryId, productId, actorMembershipId, actorDisplayName, actorMembershipStatus, targetMembershipId, targetDisplayName, targetMembershipStatus, quantity, unitPriceMinor, totalMinor, currency, productName, categoryName, createdAt, canVoid, voidReasonRequired]
      properties:
        id: { type: string }
        groupId: { type: string }
        periodId: { type: string }
        categoryId: { type: string }
        productId: { type: string }
        actorMembershipId: { type: string }
        actorDisplayName:
          type: string
          description: Server-resolved actor name so activity clients do not need the member directory.
        actorAvatarUrl:
          type: string
          description: Current protected actor avatar URL; omitted when no avatar is configured.
        actorMembershipStatus:
          type: string
          enum: [ACTIVE, ARCHIVED, DELETED]
          description: Current lifecycle state used to mark retained historical actors.
        targetMembershipId: { type: string }
        targetDisplayName:
          type: string
          description: Server-resolved charged-member name so activity clients do not need the member directory.
        targetAvatarUrl:
          type: string
          description: Current protected charged-member avatar URL; omitted when no avatar is configured.
        targetMembershipStatus:
          type: string
          enum: [ACTIVE, ARCHIVED, DELETED]
          description: Current lifecycle state used to mark retained historical booking targets.
        quantity: { type: integer }
        unitPriceMinor: { $ref: '#/components/schemas/MinorUnits' }
        totalMinor: { $ref: '#/components/schemas/MinorUnits' }
        currency: { type: string }
        productName: { type: string }
        categoryName: { type: string }
        reason: { type: string }
        createdAt: { type: string, format: date-time }
        voidedAt: { type: string, format: date-time }
        voidReason: { type: string }
        canVoid:
          type: boolean
          description: Whether the current authenticated membership may reverse this booking now.
        voidReasonRequired:
          type: boolean
          description: Whether a reversal by the current authenticated membership requires a non-empty reason.
        voidWithoutReasonUntil:
          type: string
          format: date-time
          description: End of the current actor's reason-free 30-second window; omitted for the target of an incoming booking and every unrelated booking.
    CategoryStatistic:
      type: object
      required: [categoryId, categoryName, icon, quantity, grossMinor, voidedMinor, netMinor]
      properties:
        categoryId: { type: string }
        categoryName: { type: string }
        icon: { $ref: '#/components/schemas/CategoryIcon' }
        quantity: { type: integer, format: int64 }
        grossMinor: { $ref: '#/components/schemas/MinorUnits' }
        voidedMinor: { $ref: '#/components/schemas/MinorUnits' }
        netMinor: { $ref: '#/components/schemas/MinorUnits' }
    LedgerEntry:
      type: object
      required: [id, periodId, type, amountMinor, description, createdAt]
      properties:
        id: { type: string }
        periodId: { type: string }
        bookingId: { type: string }
        paymentId: { type: string }
        reversalOf: { type: string }
        type:
          type: string
          enum: [BOOKING, PAYMENT, REVERSAL, ADJUSTMENT]
          description: Server-derived movement class; reversals take precedence over their original booking or payment reference.
        amountMinor: { $ref: '#/components/schemas/MinorUnits' }
        description: { type: string }
        createdAt: { type: string, format: date-time }
        attachment: { $ref: '#/components/schemas/PaymentAttachmentSummary' }
    AccountSummary:
      type: object
      required: [membershipId, displayName, isTemporaryGuest, status, currency, balanceMinor]
      properties:
        membershipId: { type: string }
        displayName: { type: string }
        avatarUrl: { type: string }
        isTemporaryGuest:
          type: boolean
          readOnly: true
          description: True only while the account has no login credentials.
        status: { type: string, enum: [ACTIVE, ARCHIVED, DELETED] }
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        balanceMinor: { $ref: '#/components/schemas/MinorUnits' }
    Account:
      type: object
      required: [membershipId, displayName, currency, balanceMinor, openPeriodId, openPeriodDueMinor, categoryStatistics, groupCategoryStatistics, recentEntries]
      properties:
        membershipId: { type: string }
        displayName: { type: string }
        currency: { type: string }
        balanceMinor: { $ref: '#/components/schemas/MinorUnits' }
        openPeriodId: { type: string }
        openPeriodDueMinor: { $ref: '#/components/schemas/MinorUnits' }
        categoryStatistics:
          type: array
          description: Category totals for the open period when settlements are enabled, otherwise totals across all periods.
          items: { $ref: '#/components/schemas/CategoryStatistic' }
        groupCategoryStatistics:
          type: array
          description: Anonymous category totals across the group for the open period when settlements are enabled, otherwise across all periods; an empty array without VIEW_GROUP_STATISTICS.
          items: { $ref: '#/components/schemas/CategoryStatistic' }
        recentEntries:
          type: array
          maxItems: 50
          items: { $ref: '#/components/schemas/LedgerEntry' }
    Dashboard:
      type: object
      required: [account, openPeriod, recentBookings, unreadNotificationCount]
      properties:
        account: { $ref: '#/components/schemas/Account' }
        openPeriod: { $ref: '#/components/schemas/Period' }
        recentBookings:
          type: array
          items: { $ref: '#/components/schemas/Booking' }
        unreadNotificationCount: { type: integer, format: int64 }
        groupOutstandingMinor:
          $ref: '#/components/schemas/MinorUnits'
          description: Signed complete-ledger net receivable across all member accounts; omitted without VIEW_GROUP_STATISTICS. Positive values are owed to the group and negative values are member credit.
    PaymentCommand:
      type: object
      additionalProperties: false
      required: [membershipId, amountMinor, method]
      properties:
        membershipId: { type: string }
        amountMinor: { type: integer, format: int64, minimum: 1 }
        receivedAt: { type: string, format: date-time }
        method: { type: string, minLength: 1, maxLength: 120, description: Stable identifier of a currently configured group payment method. }
        reference: { type: string, maxLength: 120, description: Required only when finance-managed payment reasons are enabled for the group. }
        note: { type: string, maxLength: 1000 }
    SelfPaymentCommand:
      type: object
      required: [amountMinor, receivedAt, method]
      additionalProperties: false
      properties:
        amountMinor: { type: integer, format: int64, minimum: 1, maximum: 100000000000 }
        receivedAt: { type: string, format: date-time }
        method: { type: string, minLength: 1, maxLength: 120, description: Stable identifier of a currently configured group payment method. }
        reference: { type: string, maxLength: 120, description: Required only when self-service payment reasons are enabled for the group. }
    PaymentAttachmentSummary:
      type: object
      additionalProperties: false
      required: [fileName, mediaType, sizeBytes, url]
      properties:
        fileName:
          type: string
          description: Normalized original filename for display and content disposition only.
        mediaType:
          type: string
          enum: [image/jpeg, image/png, application/pdf]
        sizeBytes:
          type: integer
          format: int64
          minimum: 1
        url:
          type: string
          description: Same-origin protected download route; never a storage key.
    Payment:
      type: object
      required: [id, groupId, membershipId, memberName, membershipStatus, actorMembershipId, actorDisplayName, actorMembershipStatus, amountMinor, currency, receivedAt, method, methodLabel, status, allocations]
      properties:
        id: { type: string }
        groupId: { type: string }
        membershipId: { type: string }
        memberName: { type: string }
        membershipStatus:
          type: string
          enum: [ACTIVE, ARCHIVED, DELETED]
          description: Current lifecycle state used to mark retained historical payment accounts.
        memberAvatarUrl:
          type: string
          description: Current same-origin avatar projection for the payment account membership.
        actorMembershipId:
          type: string
          description: Membership that recorded the payment.
        actorDisplayName:
          type: string
          description: Current display name of the membership that recorded the payment.
        actorMembershipStatus:
          type: string
          enum: [ACTIVE, ARCHIVED, DELETED]
          description: Current lifecycle state of the membership that recorded the payment.
        actorAvatarUrl:
          type: string
          description: Current same-origin avatar projection for the membership that recorded the payment.
        amountMinor: { $ref: '#/components/schemas/MinorUnits' }
        currency: { type: string }
        receivedAt: { type: string, format: date-time }
        method: { type: string, minLength: 1, maxLength: 120 }
        methodLabel:
          type: string
          description: Immutable display-label snapshot preserved when the configured payment method is renamed or removed.
        reference: { type: string }
        note: { type: string }
        status: { type: string, enum: [POSTED, REVERSED] }
        reversedAt: { type: string, format: date-time }
        attachment: { $ref: '#/components/schemas/PaymentAttachmentSummary' }
        allocations:
          type: array
          items:
            type: object
            required: [periodId, amountMinor]
            properties:
              periodId: { type: string }
              amountMinor: { $ref: '#/components/schemas/MinorUnits' }
    Period:
      type: object
      required: [id, groupId, label, status, startsAt]
      properties:
        id: { type: string }
        groupId: { type: string }
        label: { type: string }
        status: { type: string, enum: [OPEN, CLOSED] }
        startsAt: { type: string, format: date-time }
        closedAt: { type: string, format: date-time }
        dueAt: { type: string, format: date }
    ClosePeriodCommand:
      type: object
      required: [label, dueAt, nextPeriodLabel]
      properties:
        label: { type: string, minLength: 1, maxLength: 120 }
        dueAt: { type: string, format: date }
        nextPeriodLabel: { type: string, minLength: 1, maxLength: 120 }
    Statement:
      type: object
      required: [id, periodId, membershipId, displayName, chargesMinor, paymentsAllocatedMinor, adjustmentsAppliedMinor, adjustmentsProvidedMinor, amountDueMinor, currency, status]
      properties:
        id: { type: string }
        periodId: { type: string }
        membershipId: { type: string }
        displayName: { type: string }
        email:
          type: [string, 'null']
          format: email
          description: Close-time email snapshot; omitted or null for a credentialless temporary guest.
        chargesMinor: { $ref: '#/components/schemas/MinorUnits' }
        paymentsAllocatedMinor: { $ref: '#/components/schemas/MinorUnits' }
        adjustmentsAppliedMinor:
          $ref: '#/components/schemas/MinorUnits'
          description: Later negative corrections allocated against this period's claim.
        adjustmentsProvidedMinor:
          $ref: '#/components/schemas/MinorUnits'
          description: This period's negative corrections allocated to older claims.
        amountDueMinor: { $ref: '#/components/schemas/MinorUnits' }
        currency: { type: string }
        status: { type: string, enum: [OPEN, PARTIAL, PAID, CREDIT] }
    Notification:
      type: object
      required: [id, groupId, type, title, body, context, createdAt]
      properties:
        id: { type: string }
        groupId: { type: string }
        type:
          type: string
          enum: [BOOKING_ASSIGNED, BOOKING_REVERSED, PAYMENT_RECORDED, PAYMENT_REVERSED, SETTLEMENT_CREATED, SETTLEMENT_DUE_SOON, SETTLEMENT_OVERDUE, DATA_EXPORT_READY, DATA_EXPORT_FAILED]
        title: { type: string }
        body: { type: string }
        resourceType: { type: string }
        resourceId: { type: string }
        context: { $ref: '#/components/schemas/NotificationContext' }
        readAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
    NotificationContext:
      type: object
      additionalProperties: false
      properties:
        actorName: { type: string }
        itemName: { type: string }
        quantity: { type: integer, minimum: 1 }
        amountMinor: { $ref: '#/components/schemas/MinorUnits' }
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        periodLabel: { type: string }
        dueAt: { type: string, format: date }
        exportId: { type: string }
        exportScope: { $ref: '#/components/schemas/DataExportScope' }
    NotificationSummary:
      type: object
      additionalProperties: false
      required: [unreadCount]
      properties:
        unreadCount: { type: integer, format: int64, minimum: 0 }
    AuditEvent:
      type: object
      required: [id, action, resourceType, metadata, occurredAt]
      properties:
        id: { type: string }
        groupId: { type: string }
        actorUserId: { type: string }
        actorMembershipId: { type: string }
        actorDisplayName:
          type: string
          description: Current display name of the actor account; absent for system-generated events.
        action: { type: string }
        resourceType: { type: string }
        resourceId: { type: string }
        metadata: { type: object, additionalProperties: true }
        occurredAt: { type: string, format: date-time }
    Problem:
      type: object
      required: [type, title, status, detail, instance]
      properties:
        type: { type: string, format: uri }
        title: { type: string }
        status: { type: integer, minimum: 400, maximum: 599 }
        detail: { type: string }
        instance: { type: string }
        existingMembershipId:
          type: string
          description: Present only on a temporary-guest display-name conflict and identifies the active guest that may be explicitly reused.
    RoleConflictProblem:
      allOf:
        - $ref: '#/components/schemas/Problem'
        - type: object
          description: Assignment counts are present when deletion is blocked because an editable role is still in use; protected-role conflicts may omit them.
          properties:
            memberCount: { type: integer, format: int64, minimum: 0 }
            pendingInvitationCount: { type: integer, format: int64, minimum: 0 }
